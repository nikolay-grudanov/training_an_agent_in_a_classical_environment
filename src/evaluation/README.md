# Модуль оценки RL агентов

Модуль `src.evaluation` предоставляет инструменты для комплексной оценки производительности обученных RL агентов.

## 🎯 Основные возможности

- **Количественная оценка агентов** - расчет метрик производительности
- **Статистический анализ** - доверительные интервалы, тесты значимости
- **Сравнение агентов** - статистическое сравнение нескольких моделей
- **Генерация отчетов** - автоматическое создание отчетов в текстовом формате
- **Экспорт данных** - сохранение результатов в CSV/DataFrame
- **Воспроизводимость** - контроль случайности для повторяемых результатов

## 📊 Метрики оценки

### Основные метрики
- **Награды**: среднее, стандартное отклонение, минимум, максимум
- **Длина эпизодов**: статистики по количеству шагов
- **Доля успешных эпизодов**: процент эпизодов выше порога

### Статистические метрики
- **Доверительные интервалы** для средней награды
- **t-тесты** для сравнения агентов
- **Размер эффекта** (Cohen's d) для практической значимости

## 🚀 Быстрый старт

```python
import gymnasium as gym
from src.evaluation.evaluator import Evaluator
from src.agents.ppo_agent import PPOAgent

# Создание среды и агента
env = gym.make("CartPole-v1")
agent = PPOAgent.load("path/to/model.zip", env=env)

# Создание оценщика
evaluator = Evaluator(
    env=env,
    success_threshold=200.0,  # Порог успеха для CartPole
    confidence_level=0.95,
    random_seed=42,
)

# Оценка агента
metrics = evaluator.evaluate_agent(
    agent=agent,
    num_episodes=100,
    agent_name="PPO_CartPole",
)

print(f"Средняя награда: {metrics.mean_reward:.3f}")
print(f"Доля успешных эпизодов: {metrics.success_rate:.1%}")
```

## 📋 Основные классы

### `Evaluator`
Главный класс для оценки агентов.

**Методы:**
- `evaluate_agent()` - оценка одного агента
- `compare_agents()` - сравнение двух агентов
- `evaluate_multiple_agents()` - оценка нескольких агентов
- `generate_report()` - генерация текстового отчета
- `export_to_dataframe()` - экспорт в pandas DataFrame

### `EvaluationMetrics`
Dataclass с результатами оценки агента.

**Поля:**
- `mean_reward`, `std_reward` - статистики наград
- `mean_episode_length` - средняя длина эпизода
- `success_rate` - доля успешных эпизодов
- `reward_ci_lower`, `reward_ci_upper` - доверительный интервал
- `episode_rewards`, `episode_lengths` - детальные данные

### `ComparisonResult`
Результат сравнения двух агентов.

**Поля:**
- `better_agent` - имя лучшего агента
- `reward_ttest_pvalue` - p-value t-теста для наград
- `reward_significant` - статистическая значимость различий
- `reward_effect_size` - размер эффекта (Cohen's d)

## 🔧 Продвинутое использование

### Callback для отслеживания прогресса

```python
class ProgressCallback:
    def on_episode_start(self, episode: int) -> None:
        if episode % 10 == 0:
            print(f"Эпизод {episode}")
    
    def on_episode_end(self, episode: int, reward: float, length: int, success: bool) -> None:
        pass
    
    def on_evaluation_end(self, metrics: EvaluationMetrics) -> None:
        print(f"Оценка завершена: {metrics.mean_reward:.3f}")

# Использование
callback = ProgressCallback()
metrics = evaluator.evaluate_agent(agent, num_episodes=50, callback=callback)
```

### Сравнение агентов

```python
# Сравнение двух агентов
comparison = evaluator.compare_agents(
    agent1=ppo_agent,
    agent2=a2c_agent,
    num_episodes=100,
    agent1_name="PPO",
    agent2_name="A2C",
)

print(f"Лучший агент: {comparison.better_agent}")
print(f"Статистически значимо: {comparison.reward_significant}")
print(f"Размер эффекта: {comparison.reward_effect_size:.3f}")
```

### Пакетная оценка

```python
agents = {
    "PPO": ppo_agent,
    "A2C": a2c_agent,
    "SAC": sac_agent,
}

results = evaluator.evaluate_multiple_agents(agents, num_episodes=50)

# Генерация отчета
report = evaluator.generate_report(results)
print(report)

# Экспорт в DataFrame
df = evaluator.export_to_dataframe(results)
df.to_csv("agent_comparison.csv")
```

## 📈 Интерпретация результатов

### Доверительные интервалы
- **95% ДИ**: с 95% вероятностью истинное среднее лежит в этом интервале
- **Узкий интервал**: высокая точность оценки
- **Широкий интервал**: высокая вариативность, нужно больше эпизодов

### Статистическая значимость
- **p < 0.05**: различия статистически значимы
- **p ≥ 0.05**: различия могут быть случайными

### Размер эффекта (Cohen's d)
- **|d| < 0.2**: малый эффект
- **0.2 ≤ |d| < 0.8**: средний эффект  
- **|d| ≥ 0.8**: большой эффект

## 🎛️ Конфигурация

### Параметры Evaluator

```python
evaluator = Evaluator(
    env=env,                    # Среда для оценки
    success_threshold=200.0,    # Порог успеха (опционально)
    confidence_level=0.95,      # Уровень доверия для ДИ
    random_seed=42,             # Семя для воспроизводимости
)
```

### Параметры оценки

```python
metrics = evaluator.evaluate_agent(
    agent=agent,
    num_episodes=100,           # Количество эпизодов
    max_steps_per_episode=1000, # Лимит шагов (опционально)
    render=False,               # Отображение среды
    callback=None,              # Callback для прогресса
    agent_name="MyAgent",       # Имя для кэширования
    use_cache=True,             # Использование кэша
)
```

## 🧪 Тестирование

Запуск тестов:

```bash
# Все тесты модуля
pytest tests/unit/evaluation/ -v

# Конкретный тест
pytest tests/unit/evaluation/test_evaluator.py::TestEvaluator::test_evaluate_agent_basic -v

# С покрытием
pytest tests/unit/evaluation/ --cov=src.evaluation --cov-report=html
```

## 📝 Примеры

Полные примеры использования:
- `examples/evaluation_example.py` - базовое использование
- `examples/advanced_evaluation.py` - продвинутые возможности

## 🔍 Отладка

### Логирование
Модуль использует структурированное логирование:

```python
import logging
logging.basicConfig(level=logging.INFO)

# Теперь будут видны сообщения о прогрессе оценки
metrics = evaluator.evaluate_agent(agent, num_episodes=50)
```

### Кэширование
Результаты оценки кэшируются по ключу `{agent_name}_{num_episodes}_{max_steps}`:

```python
# Первый вызов - вычисление
metrics1 = evaluator.evaluate_agent(agent, num_episodes=50, agent_name="test")

# Второй вызов - из кэша
metrics2 = evaluator.evaluate_agent(agent, num_episodes=50, agent_name="test")

# Отключение кэша
metrics3 = evaluator.evaluate_agent(agent, num_episodes=50, use_cache=False)
```

## 🤝 Интеграция

### С другими модулями
- `src.agents.*` - все типы агентов поддерживаются
- `src.utils.seeding` - воспроизводимость результатов
- `src.utils.logging` - структурированное логирование

### С внешними инструментами
- **pandas** - экспорт результатов в DataFrame
- **scipy.stats** - статистические тесты
- **gymnasium** - совместимость со всеми средами

## 📚 API Reference

Полная документация API доступна в docstring'ах классов и методов. Используйте `help()` для получения справки:

```python
help(Evaluator.evaluate_agent)
help(EvaluationMetrics)
```