# Модуль отчетности для RL агентов

Этот модуль предоставляет инструменты для создания комплексных отчетов по результатам обучения и оценки агентов обучения с подкреплением.

## Основные возможности

### 🎯 Типы отчетов
- **Отчет по одному агенту** - детальный анализ производительности
- **Сравнительный отчет** - сопоставление нескольких агентов
- **Отчет по эксперименту** - результаты конкретного эксперимента
- **Сводный отчет** - обзор всех экспериментов

### 📊 Форматы вывода
- **HTML** - интерактивные отчеты с красивым оформлением
- **Markdown** - для документации и README
- **LaTeX** - для научных публикаций
- **JSON** - для программного анализа
- **CSV** - для табличных данных

### 🌐 Многоязычность
- Русский язык (по умолчанию)
- Английский язык
- Легко расширяемая система переводов

### 🎨 Настройка оформления
- Различные темы (default, dark, minimal)
- Настраиваемые стили
- Автоматическая вставка графиков
- Адаптивный дизайн

## Быстрый старт

```python
from src.reporting.results_formatter import ResultsFormatter, ReportConfig
from src.evaluation.evaluator import EvaluationMetrics

# Создание конфигурации
config = ReportConfig(
    language="ru",
    theme="default",
    include_plots=True,
    decimal_places=4
)

# Инициализация форматировщика
formatter = ResultsFormatter(
    output_dir="results/reports",
    config=config
)

# Создание отчета по агенту
evaluation_results = EvaluationMetrics(...)
report_path = formatter.format_single_agent_report(
    agent_name="PPO_Agent",
    evaluation_results=evaluation_results,
    output_format="html"
)
```

## Структура модуля

```
src/reporting/
├── __init__.py              # Экспорты модуля
├── results_formatter.py     # Основной класс форматировщика
├── templates/              # Шаблоны отчетов
│   ├── single_agent.html   # Шаблон отчета по агенту
│   ├── comparison.html     # Шаблон сравнительного отчета
│   └── single_agent.markdown # Markdown шаблон
└── README.md               # Документация модуля
```

## Примеры использования

### Отчет по одному агенту

```python
# HTML отчет
html_report = formatter.format_single_agent_report(
    agent_name="PPO_Agent",
    evaluation_results=metrics,
    output_format="html"
)

# Markdown отчет
md_report = formatter.format_single_agent_report(
    agent_name="PPO_Agent", 
    evaluation_results=metrics,
    output_format="markdown"
)
```

### Сравнительный отчет

```python
agents_results = {
    "PPO": ppo_metrics,
    "DQN": dqn_metrics,
    "A2C": a2c_metrics
}

comparison_report = formatter.format_comparison_report(
    agents_results=agents_results,
    output_format="html"
)
```

### Экспорт данных

```python
# CSV экспорт
csv_path = formatter.export_to_csv(
    data=agents_results,
    filename="comparison_results"
)

# JSON экспорт
json_path = formatter.export_to_json(
    data=experiment_data,
    filename="experiment_results"
)
```

## Конфигурация

### ReportConfig

```python
config = ReportConfig(
    language="ru",           # Язык отчета
    theme="default",         # Тема оформления
    include_plots=True,      # Включать графики
    include_statistics=True, # Включать статистику
    decimal_places=4,        # Знаков после запятой
    date_format="%Y-%m-%d %H:%M:%S"  # Формат даты
)
```

### Поддерживаемые темы
- `default` - стандартная тема
- `dark` - темная тема
- `minimal` - минималистичная тема

## Шаблоны

Модуль использует Jinja2 для генерации отчетов. Шаблоны поддерживают:

### Фильтры
- `format_number(places)` - форматирование чисел
- `format_percentage(places)` - форматирование процентов
- `format_datetime()` - форматирование даты/времени
- `translate(key)` - перевод текста

### Переменные шаблона
- `agent_name` - название агента
- `evaluation_results` - результаты оценки
- `config` - конфигурация отчета
- `generated_at` - время генерации
- `statistics` - дополнительная статистика

## Интеграция

Модуль интегрируется с:
- `src.evaluation.evaluator` - базовые результаты оценки
- `src.evaluation.quantitative_eval` - количественные результаты
- `src.utils.logging` - система логирования
- `pandas` - работа с табличными данными
- `jinja2` - система шаблонов

## Тестирование

```bash
# Запуск тестов
pytest tests/unit/reporting/ -v

# Запуск примера
python examples/reporting_example.py
```

## Расширение

### Добавление нового формата

1. Создайте шаблон в `templates/`
2. Добавьте обработку в `_create_default_template()`
3. Обновите документацию

### Добавление нового языка

1. Добавьте переводы в `_load_translations()`
2. Создайте шаблоны для нового языка
3. Обновите конфигурацию

### Добавление новой темы

1. Создайте CSS стили в шаблонах
2. Добавьте обработку темы в конфигурацию
3. Обновите документацию

## Лицензия

Часть проекта обучения RL агентов МИФИ.