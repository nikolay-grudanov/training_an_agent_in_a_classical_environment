# Data Model: ML Project Artifacts & Entities

**Feature**: 004-test-and-fix | **Date**: 2026-02-04
**Project Type**: Machine Learning (Reinforcement Learning) | **Phase**: 1 (Design & Contracts)

---

## 📋 NOTE: ML Project Architecture

**Это ML проект (Reinforcement Learning), NOT traditional web application.**

**Отличия от традиционных проектов:**
- ❌ **Нет REST API** - ML модели используются напрямую через Python API
- ❌ **Нет базы данных** - Данные хранятся в файлах (CSV, JSON, ZIP)
- ✅ **Есть ML модели** - Обученные агенты (PPO, A2C, TD3)
- ✅ **Есть конфигурации** - YAML/JSON конфиги для экспериментов
- ✅ **Есть артефакты обучения** - Метрики, чекпоинты, визуализации, видео

**Полную документацию см. в папке `/docs/`:
- [PROJECT_CONTEXT.md](../../docs/PROJECT_CONTEXT.md) - Обзор проекта
- [PROJECT_COMPLETION_REPORT.md](../../docs/PROJECT_COMPLETION_REPORT.md) - Финальный отчет
- [CPU_vs_GPU_Comparison.md](../../docs/CPU_vs_GPU_Comparison.md) - Сравнение CPU/GPU
- [TROUBLESHOOTING.md](../../docs/TROUBLESHOOTING.md) - Решение проблем
- [QUICKSTART.md](../../docs/QUICKSTART.md) - Быстрый старт

---

## Сущности ML проекта

### 1. Model (Обученный агент)

**Описание**: Обученный RL агент, сохраненный в формате Stable-Baselines3 (.zip)

**Поля**:
```python
{
    "model_name": "ppo_seed42",
    "algorithm": "PPO",
    "environment": "LunarLander-v3",
    "timesteps": 500000,
    "seed": 42,
    "file_path": "results/experiments/ppo_seed42/ppo_seed42_model.zip",
    "file_size": 145KB,
    "device": "cpu",
    "mean_reward": 203.15,
    "std_reward": 53.74
}
```

**Валидация**:
- Файл .zip должен быть валидным (можно загрузить через PPO.load())
- Config.json должен совпадать с параметрами обучения
- Средняя награда > 200 (целевое значение)

**Отношения**:
- Model → Checkpoint (model имеет чекпоинты)
- Model → TrainingMetrics (model имеет метрики обучения)
- Model → EvaluationMetrics (model имеет метрики оценки)

---

### 2. TrainingMetrics (Метрики обучения)

**Описание**: CSV файл с метриками обучения по всем эпизодам

**Файл**: `results/experiments/ppo_seed42/metrics.csv`

**Поля** (колонки CSV):
```csv
timesteps,episode,mean_reward,std_reward,episode_length,policy_loss,value_loss,entropy_loss,learning_rate,ent_coef
0,1,-100.5,50.2,128,0.5,1.2,0.8,0.0003,0.01
1000,10,50.3,30.1,150,0.4,1.0,0.7,0.0003,0.01
...
500000,489,203.15,53.74,180,0.1,0.5,0.3,0.0003,0.01
```

**Типы данных**:
- `timesteps`: int (количество шагов обучения)
- `episode`: int (номер эпизода)
- `mean_reward`: float (средняя награда)
- `std_reward`: float (стандартное отклонение награды)
- `episode_length`: int (длина эпизода)
- `policy_loss`, `value_loss`, `entropy_loss`: float (loss функции)
- `learning_rate`: float (текущий learning rate)
- `ent_coef`: float (entropy coefficient)

**Валидация**:
- Файл должен быть валидным CSV (может быть прочитан pandas.read_csv())
- Все числовые поля должны быть числами
- mean_reward в последней строке > 200
- std_reward в последней строке < 100

**Отношения**:
- TrainingMetrics → Model (принадлежит одной модели)

---

### 3. EvaluationMetrics (Метрики оценки)

**Описание**: CSV файл с метриками оценки модели на тестовых эпизодах

**Файл**: `results/experiments/ppo_seed42/eval_log.csv`

**Поля** (колонки CSV):
```csv
episode,episode_length,reward,mean_reward,std_reward,min_reward,max_reward
1,178,210.5,203.15,53.74,150.2,280.3
2,182,195.3,203.15,53.74,145.1,270.5
...
10,175,201.8,203.15,53.74,140.5,285.2
```

**Типы данных**:
- `episode`: int (номер эпизода оценки)
- `episode_length`: int (длина эпизода)
- `reward`: float (награда за эпизод)
- `mean_reward`, `std_reward`: float (статистика по всем эпизодам)
- `min_reward`, `max_reward`: float (мин/макс награда)

**Валидация**:
- Файл должен быть валидным CSV
- Обычно 10-20 эпизодов оценки
- mean_reward > 200
- std_reward < 100

**Отношения**:
- EvaluationMetrics → Model (принадлежит одной модели)

---

### 4. Checkpoint (Чекпоинт)

**Описание**: Промежуточное сохранение модели во время обучения

**Файл**: `results/experiments/ppo_seed42/checkpoints/rl_model_100000_steps.zip`

**Поля**:
```python
{
    "checkpoint_name": "rl_model_100000_steps",
    "timesteps": 100000,
    "file_path": "results/experiments/ppo_seed42/checkpoints/rl_model_100000_steps.zip",
    "file_size": 140KB,
    "mean_reward_at_checkpoint": 150.3
}
```

**Валидация**:
- Чекпоинты создаются каждые 100K timesteps
- Файл .zip должен быть валидным
- Средняя награда увеличивается с каждым чекпоинтом

**Отношения**:
- Checkpoint → Model (принадлежит одной модели)

---

### 5. Configuration (Конфигурация эксперимента)

**Описание**: JSON файл с параметрами обучения

**Файл**: `results/experiments/ppo_seed42/config.json`

**Поля**:
```json
{
    "algorithm": "PPO",
    "environment": "LunarLander-v3",
    "timesteps": 500000,
    "seed": 42,
    "gamma": 0.999,
    "ent_coef": 0.01,
    "gae_lambda": 0.98,
    "n_steps": 1024,
    "n_epochs": 4,
    "batch_size": 64,
    "learning_rate": 0.0003,
    "device": "cpu",
    "n_envs": 1,
    "created_at": "2026-02-04T12:00:00Z"
}
```

**Типы данных**:
- `algorithm`: string ("PPO", "A2C", "TD3")
- `environment`: string ("LunarLander-v3")
- `timesteps`, `seed`, `n_steps`, `n_epochs`, `batch_size`: int
- `gamma`, `ent_coef`, `gae_lambda`, `learning_rate`: float
- `device`: string ("cpu", "auto", "cuda")
- `created_at`: datetime (ISO 8601)

**Валидация**:
- Файл должен быть валидным JSON
- Все параметры должны быть валидными значениями
- Параметры должны соответствовать RL Zoo best practices (gamma=0.999, ent_coef=0.01)

**Отношения**:
- Configuration → Model (определяет параметры обучения модели)

---

### 6. Visualization (Визуализация)

**Описание**: Графики и видео для анализа обучения

**Файлы**:
- `reward_curve.png` - Кривая обучения (награда vs timesteps)
- `loss_curves.png` - Кривые loss функций
- `episode_length.png` - Длина эпизодов vs timesteps
- `video.mp4` - Демонстрация обученного агента

**Поля**:
```python
{
    "visualization_name": "reward_curve",
    "type": "plot",
    "file_path": "results/experiments/ppo_seed42/reward_curve.png",
    "file_size": 57KB,
    "format": "PNG",
    "created_at": "2026-02-04T12:05:00Z"
}
```

**Типы визуализаций**:
- `plot`: PNG графики (matplotlib)
- `video`: MP4 видео (gymnasium录制)

**Валидация**:
- PNG файлы должны быть валидными изображениями
- MP4 видео должно воспроизводиться
- Графики должны показывать сходимость (reward > 200)

**Отношения**:
- Visualization → Model (визуализирует обучение одной модели)

---

## Структура хранилища (Storage Layout)

```
results/
├── experiments/                 # Директория для каждого эксперимента
│   └── ppo_seed42/          # Эксперимент PPO с seed=42
│       ├── ppo_seed42_model.zip          # Model
│       ├── metrics.csv                    # TrainingMetrics
│       ├── eval_log.csv                   # EvaluationMetrics
│       ├── config.json                    # Configuration
│       ├── reward_curve.png                # Visualization (plot)
│       ├── loss_curves.png               # Visualization (plot)
│       ├── episode_length.png             # Visualization (plot)
│       ├── video.mp4                     # Visualization (video)
│       └── checkpoints/                  # Checkpoints
│           ├── rl_model_100000_steps.zip
│           ├── rl_model_200000_steps.zip
│           ├── rl_model_300000_steps.zip
│           └── rl_model_400000_steps.zip
├── comparison/                  # Результаты сравнительных экспериментов
│   ├── cpu_vs_gpu/             # Сравнение CPU vs GPU
│   │   ├── metrics.csv
│   │   └── comparison.png
│   └── parameter_tuning/        # Результаты тюнинга параметров
│       ├── lr_3e4/metrics.csv
│       ├── lr_1e4/metrics.csv
│       └── comparison.png
└── reports/                    # Генерируемые отчеты
    ├── training_report_20260204.md
    └── performance_report_20260204.md
```

---

## Схема отношений (Entity Relationship)

```
┌─────────────────┐         ┌──────────────────────┐
│    Model        │─────────▶│  Configuration      │
│  (ppo_*.zip)   │         │  (config.json)      │
└─────────────────┘         └──────────────────────┘
        │                            │
        │ 1                          │ 1
        │                            │
        │                            │
        │                            │
        │ 1                          │
        ▼                            │
┌─────────────────┐                 │
│  Checkpoint     │◀────────────────│
│ (rl_model_*.zip)│                 │
└─────────────────┘                 │
                                   │
┌─────────────────┐                 │
│TrainingMetrics  │◀────────────────│
│  (metrics.csv)  │                 │
└─────────────────┘                 │
                                   │
┌─────────────────┐                 │
│EvaluationMetrics│◀────────────────│
│ (eval_log.csv)  │                 │
└─────────────────┘                 │
                                   │
┌─────────────────┐                 │
│  Visualization  │◀────────────────│
│ (*.png, *.mp4) │                 │
└─────────────────┘                 │
                                   │
                                   │
                                   │
                            (определяет)
                                   │
                                   │
                                   │
                            ┌─────┴─────┐
                            │    Seed    │
                            │   (42)     │
                            └───────────┘
```

**Legend**:
- 1: One-to-one relationship
- ▶: Reference / points to
- ◀: Belongs to

---

## Валидационные правила

### Model Validation
```python
def validate_model(model_path: str, config_path: str) -> bool:
    """
    Валидация обученной модели
    """
    # 1. Проверить файл существует
    assert os.path.exists(model_path), "Model file not found"

    # 2. Проверить размер файла > 0
    assert os.path.getsize(model_path) > 0, "Model file is empty"

    # 3. Попытаться загрузить модель
    model = PPO.load(model_path)
    assert model is not None, "Failed to load model"

    # 4. Проверить конфигурацию
    with open(config_path) as f:
        config = json.load(f)
    assert config["mean_reward"] > 200, "Mean reward < 200"

    return True
```

### TrainingMetrics Validation
```python
def validate_metrics(metrics_path: str) -> bool:
    """
    Валидация метрик обучения
    """
    # 1. Проверить CSV файл читается
    df = pd.read_csv(metrics_path)

    # 2. Проверить все колонки существуют
    required_cols = ["timesteps", "episode", "mean_reward", "std_reward"]
    assert all(col in df.columns for col in required_cols), "Missing columns"

    # 3. Проверить последнюю строку
    last_row = df.iloc[-1]
    assert last_row["mean_reward"] > 200, "Final reward < 200"
    assert last_row["std_reward"] < 100, "Final std > 100"

    return True
```

---

## Migration Notes

**Нет миграций** - Это ML проект, данные хранятся в файлах, не в базе данных.

**Артефакты версионируются** через git (код) и gitignore (результаты):
- Код: Версионирован в Git (src/, tests/, docs/)
- Результаты: Не версионированы (results/ в .gitignore)
- Документация: Версионирована в Git (docs/)

---

## Ссылки на документацию

**Полная документация проекта** (см. папку `/docs/`):

- 📄 [PROJECT_CONTEXT.md](../../docs/PROJECT_CONTEXT.md) - Обзор проекта и контекст
- 📄 [PROJECT_COMPLETION_REPORT.md](../../docs/PROJECT_COMPLETION_REPORT.md) - Финальный отчет о проекте
- 📄 [CPU_vs_GPU_Comparison.md](../../docs/CPU_vs_GPU_Comparison.md) - Детальное сравнение CPU и GPU обучения
- 📄 [TROUBLESHOOTING.md](../../docs/TROUBLESHOOTING.md) - Решение типичных проблем
- 📄 [QUICKSTART.md](../../docs/QUICKSTART.md) - Быстрый старт и основные команды

**Планы тестирования**:

- 📋 [004-test-and-fix-experiments.md](../../004-test-and-fix-experiments.md) - План из 13 фаз финального тестирования
- 📋 [specs/004-test-and-fix/spec.md](./spec.md) - Спецификация фичи

---

## Заключение

Этот документ описывает data model для ML проекта (Reinforcement Learning), где:
- **Единицы сущностей**: ML модели, метрики, конфигурации, чекпоинты, визуализации
- **Хранилище**: Файловая система (CSV, JSON, ZIP, PNG, MP4)
- **Нет API**: Использование через Python API (Stable-Baselines3)
- **Нет базы данных**: Данные в файлах, версионирование через Git

Для полной документации см. папку `/docs/`.
