# QUICKSTART.md - Быстрый старт 🚀

Этот документ поможет вам начать работу с проектом **за 5 минут**!

---

## ⚡ 30-секундный старт (TUI)

Самый быстрый способ начать:

```bash
# 1. Активировать окружение
conda activate rocm

# 2. Запустить интерактивное меню
python run_experiments.py

# 3. Выбрать опцию 8 (Полный workflow) и нажать Enter
```

**Всё!** Скрипт автоматически:
- Обучит PPO и A2C модели
- Сгенерирует графики
- Создаст видео демонстрации
- Сгенерирует итоговый отчёт

---

## 🎯 5-минутный старт (Прямые команды)

Если вы предпочитаете прямые команды:

```bash
# 1. Активировать окружение
conda activate rocm

# 2. Обучить PPO модель (200K шагов, ~5 минут)
python -m src.experiments.completion.baseline_training \
    --algo ppo --timesteps 200000 --seed 42

# 3. Обучить A2C модель (200K шагов, ~4 минуты)
python -m src.experiments.completion.baseline_training \
    --algo a2c --timesteps 200000 --seed 42

# 4. Оценить модели
python -c "
from src.training.evaluation import evaluate_agent
for exp_id in ['ppo_seed42', 'a2c_seed42']:
    result = evaluate_agent(f'results/experiments/{exp_id}/{exp_id}_model.zip')
    print(f'{exp_id}: {result[\"mean_reward\"]:.2f} ± {result[\"std_reward\"]:.2f}')
"

# 5. Сгенерировать видео
python -m src.visualization.video \
    --model results/experiments/ppo_seed42/ppo_seed42_model.zip \
    --output video.mp4 --episodes 5
```

---

## 📂 Структура результатов

После обучения вы увидите:

```
results/
├── experiments/
│   ├── ppo_seed42/
│   │   ├── ppo_seed42_model.zip       # Обученная модель
│   │   ├── config.json                # Конфигурация
│   │   ├── metrics.csv               # Метрики обучения
│   │   ├── eval_log.csv              # Логи оценки
│   │   └── reward_curve.png          # График обучения
│   └── a2c_seed42/
│       └── ... (аналогичная структура)
├── comparison/
│   ├── a2c_vs_ppo.png              # Сравнительный график
│   └── gamma_comparison.png          # Сравнение gamma
└── reports/
    └── experiment_report.md          # Отчёт
```

---

## 🚀 Автоматический запуск всех экспериментов

Хотите запустить **ВСЁ** автоматически?

```bash
# Быстрый режим (50K шагов для тестирования)
./scripts/run_all_experiments.sh --quick

# Стандартный режим (200K шагов)
./scripts/run_all_experiments.sh

# Полный режим (500K шагов для сходимости)
./scripts/run_all_experiments.sh --full
```

**Скрипт автоматически:**
1. ✅ Обучит все модели (PPO, A2C, gamma)
2. ✅ Сгенерирует все графики
3. ✅ Создаст все видео
4. ✅ Сгенерирует отчёты

---

## 📊 Анализ результатов

После экспериментов:

```bash
# Список всех экспериментов
python scripts/results_summarizer.py --list

# Сводка по конкретному эксперименту
python scripts/results_summarizer.py --experiment ppo_seed42

# Сравнение экспериментов
python scripts/results_summarizer.py --compare ppo_seed42 a2c_seed42

# Полный анализ
./scripts/analyze_results.sh
```

---

## 🔍 Проверка артефактов

Убедитесь, что всё создано корректно:

```bash
# Проверить конкретный эксперимент
python scripts/verify_artifacts.py --experiment ppo_seed42

# Проверить все эксперименты
python scripts/verify_artifacts.py --all
```

---

## 🧪 Тестирование

Запустите тесты, чтобы убедиться, что всё работает:

```bash
# Все unit-тесты
pytest tests/unit/ -v

# Интеграционные тесты
pytest tests/integration/ -v

# С покрытием кода
pytest tests/ -v --cov=src/ --cov-report=html
```

---

## ✅ Проверка качества кода

Перед коммитом всегда проверяйте код:

```bash
# Автоисправление ошибок
ruff check . --fix

# Форматирование
ruff format .

# Проверка типов
mypy src/ --strict
```

**Или всё сразу:**
```bash
ruff check . --fix && ruff format . && mypy src/ --strict
```

---

## 🎨 TUI Меню

Запустите `python run_experiments.py` и выберите:

| Опция | Описание |
|-------|----------|
| 1 | 🎓 Обучение моделей (PPO/A2C) |
| 2 | 📊 Оценка моделей |
| 3 | 📈 Генерация графиков |
| 4 | 🎬 Генерация видео |
| 5 | 📋 Генерация отчётов |
| 6 | 🧪 Тестирование |
| 7 | ✅ Проверка качества кода |
| 8 | 🚀 **Полный workflow** (автоматически всё делает) |

---

## ⚠️ Возможные проблемы

### Ошибка: `conda activate: command not found`

**Решение:**
```bash
# В Linux bash
source ~/anaconda3/etc/profile.d/conda.sh
conda activate rocm

# Или используйте полный путь к python
~/anaconda3/envs/rocm/bin/python run_experiments.py
```

### Ошибка: `ModuleNotFoundError: No module named 'stable_baselines3'`

**Решение:**
```bash
pip install -r requirements.txt
# или
pip install stable-baselines3 gymnasium
```

### Ошибка: Память не хватает

**Решение:**
```bash
# Используйте меньше эпизодов для видео
python -m src.visualization.video \
    --model results/experiments/ppo_seed42/ppo_seed42_model.zip \
    --output video.mp4 --episodes 2
```

---

## 📚 Дополнительная документация

- **[КОМАНДЫ.md](КОМАНДЫ.md)** - Полный справочник команд
- **[PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)** - Контекст проекта
- **[AGENTS.md](AGENTS.md)** - Руководство для агентов
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - Решение проблем

---

## 🎉 Вы готовы!

Теперь вы можете:
- ✅ Обучать модели через TUI или команды
- ✅ Сравнивать эксперименты
- ✅ Анализировать результаты
- ✅ Генерировать отчёты

**Удачи с экспериментами!** 🚀

---

*Время чтения: 5 минут | Обновлено: 2026-02-03*
