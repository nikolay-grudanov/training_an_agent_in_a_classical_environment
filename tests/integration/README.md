# Интеграционные тесты контролируемых экспериментов

Этот пакет содержит комплексные интеграционные тесты для системы контролируемых экспериментов, которые проверяют работу полного пайплайна от создания эксперимента до анализа результатов.

## 🎯 Цель тестов

Интеграционные тесты проверяют:
- **Полный пайплайн экспериментов**: от конфигурации до результатов
- **Интеграцию компонентов**: Experiment, ExperimentRunner, Comparison
- **Реальные сценарии использования**: PPO vs A2C сравнение
- **CLI интерфейс**: командная строка и различные режимы
- **Обработку ошибок**: валидация, восстановление, очистка
- **Производительность**: время выполнения, использование ресурсов

## 📁 Структура тестов

```
tests/integration/
├── __init__.py                     # Инициализация пакета
├── test_controlled_experiments.py # Основные интеграционные тесты
├── test_cli_interface.py          # Тесты CLI интерфейса
└── README.md                      # Этот файл
```

## 🧪 Основные тестовые сценарии

### test_controlled_experiments.py

**TestControlledExperiments** - главный класс интеграционных тестов:

#### Базовые тесты
- `test_experiment_creation_from_config()` - создание эксперимента из конфигурации
- `test_experiment_configuration_validation()` - валидация конфигураций
- `test_experiment_lifecycle_management()` - управление жизненным циклом

#### Выполнение экспериментов
- `test_ppo_vs_a2c_experiment_execution()` - **основной тест** выполнения PPO vs A2C
- `test_parallel_execution_mode()` - тест параллельного выполнения
- `test_validation_mode()` - тест режима валидации (dry-run)

#### Анализ результатов
- `test_experiment_results_collection()` - сбор результатов
- `test_statistical_comparison_analysis()` - статистический анализ
- `test_file_output_validation()` - валидация выходных файлов

#### Обработка ошибок
- `test_configuration_validation_and_error_handling()` - обработка ошибок конфигурации
- `test_error_recovery_and_cleanup()` - восстановление после ошибок

#### Производительность
- `test_performance_validation()` - валидация производительности
- `test_memory_and_resource_usage()` - использование ресурсов
- `test_deterministic_results_with_seeds()` - детерминированность результатов

#### Полный пайплайн
- `test_full_pipeline_integration()` - **комплексный тест** всего пайплайна
- `test_documentation_examples_work()` - проверка примеров из документации

### test_cli_interface.py

**TestCLIInterface** - тесты командной строки:

#### Базовые CLI тесты
- `test_config_file_validation()` - валидация конфигурационных файлов
- `test_cli_help_command()` - команда помощи
- `test_cli_argument_parsing()` - парсинг аргументов

#### Режимы выполнения
- `test_cli_validation_mode()` - режим валидации через CLI
- `test_cli_error_handling()` - обработка ошибок CLI
- `test_cli_verbose_levels()` - уровни детализации вывода

#### Конфигурация
- `test_cli_output_directory_creation()` - создание выходных директорий
- `test_cli_configuration_override()` - переопределение параметров
- `test_cli_environment_variables()` - переменные окружения

#### Полное тестирование
- `test_cli_full_experiment_simulation()` - симуляция полного эксперимента

## ⚙️ Конфигурация тестов

### Тестовая конфигурация

Используется `configs/test_ppo_vs_a2c.yaml` с параметрами для быстрого тестирования:

```yaml
baseline:
  algorithm: "PPO"
  training_steps: 5000  # Короткое обучение
  evaluation_frequency: 1000

variant:
  algorithm: "A2C"
  training_steps: 5000  # Короткое обучение
  evaluation_frequency: 1000

evaluation:
  num_episodes: 5  # Минимальное количество
  
experiment:
  output:
    save_videos: false  # Отключено для скорости
```

### Маркеры pytest

- `@pytest.mark.integration` - интеграционные тесты
- `@pytest.mark.slow` - медленные тесты с реальным обучением
- `@pytest.mark.cli` - тесты CLI интерфейса

## 🚀 Запуск тестов

### Быстрые команды (через Makefile)

```bash
# Все интеграционные тесты (быстрые)
make test-integration-fast

# Тесты контролируемых экспериментов
make test-controlled-experiments

# Тесты CLI
make test-cli

# Все интеграционные тесты (включая медленные)
make test-integration
```

### Прямые команды pytest

```bash
# Все интеграционные тесты
pytest tests/integration/ -v

# Только быстрые интеграционные тесты
pytest tests/integration/ -m "integration and not slow" -v

# Конкретный тестовый файл
pytest tests/integration/test_controlled_experiments.py -v

# Конкретный тест
pytest tests/integration/test_controlled_experiments.py::TestControlledExperiments::test_full_pipeline_integration -v

# С покрытием кода
pytest tests/integration/ --cov=src --cov-report=html
```

### Параметры запуска

```bash
# Увеличенный таймаут для медленных тестов
pytest tests/integration/ --timeout=300

# Подробный вывод
pytest tests/integration/ -v -s

# Остановка на первой ошибке
pytest tests/integration/ -x

# Параллельный запуск (если установлен pytest-xdist)
pytest tests/integration/ -n auto
```

## 📊 Ожидаемые результаты

### Успешное выполнение

При успешном выполнении тесты должны:

1. **Создать эксперимент** с валидными конфигурациями PPO и A2C
2. **Выполнить обучение** обеих конфигураций (5000 шагов каждая)
3. **Собрать результаты** с метриками производительности
4. **Провести сравнение** с статистическим анализом
5. **Сохранить файлы** эксперимента, модели, отчеты
6. **Завершиться** в разумное время (< 5 минут для быстрых тестов)

### Примерный вывод

```
✅ Эксперимент создан
✅ Валидация конфигураций прошла успешно
✅ Эксперимент выполнен за 180.5с
📊 PPO финальная награда: -85.23
📊 A2C финальная награда: -92.17
✅ Статистическое сравнение выполнено
✅ Результаты сохранены: /tmp/test_experiments_*/experiment_*.json
✅ Выходные файлы созданы
🎉 Полный интеграционный тест завершен успешно!
```

## 🐛 Отладка тестов

### Логирование

Тесты используют подробное логирование:

```bash
# Включить логирование pytest
pytest tests/integration/ --log-cli-level=INFO

# Сохранить логи в файл
pytest tests/integration/ --log-file=test.log
```

### Сохранение артефактов

При ошибках тесты сохраняют:
- Конфигурации экспериментов
- Логи выполнения
- Частичные результаты
- Состояние системы

### Общие проблемы

1. **Таймауты**: Увеличьте `--timeout` для медленных систем
2. **Память**: Проверьте доступную RAM (тесты требуют ~2GB)
3. **Зависимости**: Убедитесь, что установлены все пакеты
4. **Права доступа**: Проверьте права на запись в директории результатов

## 🔧 Настройка окружения

### Минимальные требования

- Python 3.10+
- 4GB RAM
- 2GB свободного места на диске
- Установленные зависимости из `requirements.txt`

### Переменные окружения

```bash
export PYTHONPATH=.
export TESTING=1
export EXPERIMENT_OUTPUT_DIR=tests/tmp
export LOG_LEVEL=INFO
```

### Установка зависимостей

```bash
# Основные зависимости
pip install -r requirements.txt

# Зависимости для тестирования
pip install pytest pytest-cov pytest-mock pytest-timeout

# Установка проекта в режиме разработки
pip install -e .
```

## 📈 Метрики и покрытие

### Целевые метрики

- **Покрытие кода**: > 80% для интеграционных компонентов
- **Время выполнения**: < 5 минут для быстрых тестов
- **Успешность**: 100% прохождение на чистой системе
- **Детерминированность**: Одинаковые результаты при фиксированных seeds

### Анализ покрытия

```bash
# Генерация отчета покрытия
pytest tests/integration/ --cov=src --cov-report=html

# Просмотр отчета
open htmlcov/index.html
```

## 🤝 Вклад в тесты

### Добавление новых тестов

1. Следуйте существующей структуре классов
2. Используйте фикстуры для настройки
3. Добавляйте подробные docstring
4. Используйте соответствующие маркеры pytest
5. Обеспечьте изоляцию тестов

### Соглашения

- Имена тестов: `test_<функциональность>_<сценарий>()`
- Классы тестов: `Test<Компонент>`
- Фикстуры: `<компонент>_<назначение>`
- Временные файлы: используйте `tempfile`

### Пример нового теста

```python
def test_new_functionality_basic_scenario(self, test_experiment: Experiment):
    """Тест новой функциональности в базовом сценарии."""
    # Подготовка
    setup_data = create_test_data()
    
    # Выполнение
    result = test_experiment.new_method(setup_data)
    
    # Проверки
    assert result is not None
    assert result.success
    assert result.metrics["key_metric"] > expected_threshold
    
    print(f"✅ Новая функциональность работает: {result.summary}")
```

## 📚 Дополнительные ресурсы

- [Документация pytest](https://docs.pytest.org/)
- [Руководство по интеграционному тестированию](https://martinfowler.com/bliki/IntegrationTest.html)
- [Best practices для ML тестирования](https://madewithml.com/courses/mlops/testing/)

---

**Примечание**: Эти тесты являются критически важными для обеспечения качества системы контролируемых экспериментов. Регулярно запускайте их при изменениях в коде и следите за временем выполнения.