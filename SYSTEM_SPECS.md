# Характеристики системы (SYSTEM_SPECS.md)


**Цель документа:** Фиксация конфигурации системы для воспроизводимости RL-экспериментов на LunarLander-v3. Анализ производительности обучения PPO/A2C. Рекомендации по CPU vs GPU.  
**Связанные документы:** [CPU_vs_GPU_Comparison.md](CPU_vs_GPU_Comparison.md)

Эта система:
- 32 потока CPU,
-  62GB RAM,
-   двойная GPU AMD с ROCm 6.3.

## 1. Общая информация 🖥️

| Параметр | Значение |
|----------|----------|
| **ОС** | Manjaro Linux x86_64 ✅ |
| **Хост** | MS-7E12 1.0 (MSI) |
| **Ядро** | 6.18.4-1-MANJARO |
| **DE** | GNOME 49.2 |
| **Shell** | zsh 5.9 |


## 2. CPU

**Модель:** AMD Ryzen 9 9950X 16-Core Processor  
**Характеристики:**
- Ядра: 16 физических, **32 потока (SMT)** ✅ (отлично для параллельного RL-обучения в SB3)
- Базовая частота: **5.756 GHz** (турбо)
- Масштабирование: 78% (текущее)

**Сравнение с RL-требованиями:**  
LunarLander-v3 (MLP политики) требует ~4-8 ядер. Здесь **overkill** ✅ — обучение 1M+ шагов за минуты на CPU.

**Команда для проверки:**
```bash
lscpu | grep -E 'Model name|Core|Thread|MHz'
```

## 3. GPU

**ROCm 6.3.42134-a9a80e791 установлен и работает** ✅ (PyTorch: `torch.cuda.is_available() == True`)

| Устройство | Модель | Темп. | Потребл. | SCLK/MCLK | Fan | Perf | VRAM | GPU Util | Лимит |
|------------|--------|-------|----------|-----------|-----|------|------|----------|-------|
| **Device 0** | Radeon RX 7700 XT (GUID: 0x747e) | 35°C | 11W | 27/96 MHz | 0% | 0 (idle) | 7% | 1% | 228W ✅ |
| **Device 1** | Radeon RX 7800 XT (GUID: 0x13c0) | 47°C | 0.02W | -/3000 MHz | 0% | 0 (idle) | - | 0% | N/A ⚠️ |
| **iGPU** | Radeon Graphics | - | - | - | - | - | - | - | - |

**Сравнение:** ROCm ускоряет CNN, но для **MLP в LunarLander** overhead > польза (см. [CPU_vs_GPU_Comparison.md](CPU_vs_GPU_Comparison.md)).  
**Команда:**
```bash
rocm-smi
```

## 4. Память

| Параметр | Значение |
|----------|----------|
| **Всего** | **62 GB** (61878 MiB) ✅ |
| **Активно** | 11 GB (11168 MiB) |
| **Доступно** | 48 GB |
| **Swap** | 66 GB (0% использован) |

**Оценка:** Достаточно для батчей 4096+, нескольких моделей, видео (MP4). Нет OOM при обучении.

**Команда:**
```bash
free -h
```

## 5. Хранение

| Диск | Размер | Использовано | Свободно | Заполнено |
|------|--------|--------------|----------|-----------|
| **/dev/nvme1n1p2 (NVMe SSD)** | 1.8 TB | 1.1 TB | **602 GB** ✅ | 65% |

**Оценка:** Много места для `results/` (логи, модели .zip, CSV, PNG, MP4).

**Команда:**
```bash
df -h /
```

## 6. Программное обеспечение 

| Пакет | Версия |
|-------|--------|
| **Python** | 3.10|
| **PyTorch** | 2.9.1+rocm6.3 |
| **ROCm (HIP)** | 6.3.42134-a9a80e791 |
| **NumPy** | 1.26.4 |
| **Stable-Baselines3** | 2.7.1 |
| **Gymnasium** | 1.2.3 (Box2D) |
| **Matplotlib** | 3.9.4 |
| **Torchvision** | 0.24.1+rocm6.3 |
| **Triton (ROCm)** | 3.5.1 |

**Проверка PyTorch+ROCm:**
```python
import torch
print(f"CUDA/ROCm: {torch.cuda.is_available()}")
print(torch.__version__)
```

## 7. RL-окружение для LunarLander-v3 🎯

- **Эффективность обучения PPO:**  
  - CPU (32 потока): **~1M шагов / 2-5 мин** ✅ (рекомендуется для MLP).
  - GPU (ROCm): **Медленнее на 20-50%** из-за overhead (см. [CPU_vs_GPU_Comparison.md](CPU_vs_GPU_Comparison.md)).
- **Целевая производительность:** Reward >200 (достигнуто в экспериментах).
- **Параллелизм:** `n_envs=16-32` на CPU.

**Команда запуска:**
```bash
python -m src.training.trainer --config configs/training_schema.yaml --device cpu
```