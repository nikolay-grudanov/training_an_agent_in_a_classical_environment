# 004-test-and-fix-experiments.md: План финального тестирования, отладки и оптимизации RL проекта

## 1. Обзор и цели

**Обзор документа:**
- Цель: Финальная проверка, тестирование и отладка всей системы
- Статус проекта: Основные задачи выполнены (агент обучен, награда > 200)
- Задача этого этапа: Убедиться, что всё работает надежно и исправить любые проблемы

**Цели:**
- ✅ Верифицировать все компоненты системы
- ✅ Прогнать полный пайплайн обучения
- ✅ Исправить все найденные баги
- ✅ Оптимизировать параметры при необходимости
- ✅ Обеспечить воспроизводимость результатов
- ✅ Создать финальную документацию использования

## 2. Чеклист задач

## Чеклист финального этапа

### Фаза 1: Верификация окружения
- [ ] Проверить все зависимости установлены
- [ ] Проверить версию Python (3.10.14)
- [ ] Проверить conda environment &quot;rocm&quot; активен
- [ ] Проверить PyTorch + ROCm работает
- [ ] Проверить Stable-Baselines3 установлен
- [ ] Проверить Gymnasium работает с LunarLander-v3

### Фаза 2: Тестирование базового пайплайна
- [ ] Запустить PPO обучение с default параметрами (200K)
- [ ] Проверить модель сохраняется корректно
- [ ] Проверить метрики записываются в CSV
- [ ] Проверить чекпоинты создаются
- [ ] Проверить визуализации генерируются

### Фаза 3: Тестирование оптимальных параметров
- [ ] Запустить PPO с оптимизированными параметрами (500K, CPU)
- [ ] Проверить награда &gt;= 200
- [ ] Проверить обучение на CPU работает
- [ ] Проверить отсутствие GPU предупреждений
- [ ] Сравнить время с предыдущими запусками

### Фаза 4: Тестирование загрузки и инференса
- [ ] Загрузить сохраненную модель
- [ ] Запустить 10 эпизодов инференса
- [ ] Проверить средняя награда &gt; 200
- [ ] Проверить детерминированный режим работает
- [ ] Проверить визуализация эпизода

### Фаза 5: Юнит-тесты
- [ ] Прогнать все unit tests
- [ ] Зафиксировать количество passed/failed
- [ ] Проанализировать failed тесты
- [ ] Исправить критические failed тесты
- [ ] Перепрогнать тесты после исправлений

### Фаза 6: Интеграционные тесты
- [ ] Прогнать integration tests
- [ ] Проверить полный пайплайн работает
- [ ] Проверить артефакты создаются
- [ ] Проверить метрики корректны
- [ ] Проверить конфигурации работают

### Фаза 7: Качество кода
- [ ] Ruff linting (ruff check .)
- [ ] Ruff formatting (ruff format .)
- [ ] Проверить type hints
- [ ] Проверить docstrings
- [ ] Проверить PEP 8 соответствие

### Фаза 8: Производительность
- [ ] Замерить время обучения (50K steps)
- [ ] Замерить время обучения (500K steps)
- [ ] Проверить CPU vs GPU скорость
- [ ] Замерить память
- [ ] Проверить нет утечек памяти

### Фаза 9: Воспроизводимость
- [ ] Запустить обучение с seed=42 дважды
- [ ] Проверить идентичные результаты
- [ ] Запустить с разными seed (123, 999)
- [ ] Проверить seed влияет на результат
- [ ] Зафиксировать результаты для каждого seed

### Фаза 10: Отладка и исправление
- [ ] Составить список найденных багов
- [ ] Приоритизировать баги (critical/high/medium/low)
- [ ] Исправить critical баги
- [ ] Исправить high приоритет баги
- [ ] Проверить исправления

### Фаза 11: Оптимизация параметров (если нужно)
- [ ] Анализировать текущие результаты
- [ ] Если награда &lt; 200, экспериментировать с параметрами
- [ ] Пробовать different learning rates
- [ ] Пробовать different ent_coef values
- [ ] Пробовать different n_steps/n_epochs
- [ ] Запустить финальный эксперимент

### Фаза 12: Документация
- [ ] Обновить README с командами
- [ ] Обновить TROUBLESHOOTING.md
- [ ] Обновить PROJECT_CONTEXT.md
- [ ] Создать QUICKSTART.md (если нет)
- [ ] Проверить все ссылки работают

### Фаза 13: Финальная проверка
- [ ] Прогнать полный пайплайн с нуля
- [ ] Проверить все артефакты создаются
- [ ] Проверить все тесты проходят
- [ ] Проверить документация полная
- [ ] Создать финальный отчет о статусе

## 3. Детальный план по фазам

### Фаза 1: Верификация окружения

**Цель:** Убедиться, что все зависимости и окружение настроены правильно.

**Команды для проверки:**

```bash
# 1. Проверить Python версию
python --version
# Ожидаемый вывод: Python 3.10.14

# 2. Проверить conda environment
conda info --envs
# Ожидаемая активная среда: rocm

# 3. Проверить PyTorch и ROCm
python -c &quot;import torch; print(f&#x27;PyTorch: {torch.__version__}&#x27;); print(f&#x27;ROCm: {torch.cuda.is_available()}&#x27;); print(f&#x27;GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \&quot;N/A\&quot;}&#x27;)&quot;
# Ожидаемый вывод: PyTorch 2.5.1+rocm6.2, ROCm: True, GPU: AMD Radeon RX 7800 XT

# 4. Проверить Stable-Baselines3
python -c &quot;import stable_baselines3; print(f&#x27;SB3 version: {stable_baselines3.__version__}&#x27;)&quot;
# Ожидаемый вывод: SB3 version: 2.7.1

# 5. Проверить Gymnasium
python -c &quot;import gymnasium as gym; env = gym.make(&#x27;LunarLander-v3&#x27;); print(f&#x27;Environment: {env}&#x27;); env.close()&quot;
# Ожидаемый вывод: Environment: &lt;LunarLander-v3&gt;

# 6. Проверить все зависимости
pip list | grep -E &quot;torch|stable-baselines3|gymnasium|numpy|matplotlib&quot;
```

**Критерии успеха:**
- ✅ Python 3.10.14
- ✅ Conda environment &quot;rocm&quot; активен
- ✅ PyTorch 2.5.1+rocm6.2
- ✅ ROCm работает (torch.cuda.is_available() == True)
- ✅ Stable-Baselines3 2.7.1
- ✅ Gymnasium работает с LunarLander-v3

**Если ошибка:**
- Проверить `requirements.txt`
- Установить недостающие зависимости: `pip install -r requirements.txt`
- Пересоздать conda environment если нужно

### Фаза 2: Тестирование базового пайплайна

**Цель:** Проверить, что базовый пайплайн обучения работает с default параметрами.

**Команда:**
```bash
# Запустить PPO с default параметрами (200K timesteps)
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 200000 \
    --seed 42
```

**Ожидаемые результаты:**
- Время обучения: ~5 минут
- Средняя награда: ~50-100 (baseline)
- Файлы созданы:
  - `results/experiments/ppo_seed42/ppo_seed42_model.zip`
  - `results/experiments/ppo_seed42/metrics.csv`
  - `results/experiments/ppo_seed42/eval_log.csv`
  - `results/experiments/ppo_seed42/config.json`
  - `results/experiments/ppo_seed42/checkpoints/` (4 чекпоинта)

**Проверки после обучения:**
```bash
# 1. Проверить модель существует
ls -lh results/experiments/ppo_seed42/ppo_seed42_model.zip

# 2. Проверить метрики CSV
head -20 results/experiments/ppo_seed42/metrics.csv

# 3. Проверить eval log CSV
head -20 results/experiments/ppo_seed42/eval_log.csv

# 4. Проверить чекпоинты
ls -lh results/experiments/ppo_seed42/checkpoints/

# 5. Проверить конфигурацию
cat results/experiments/ppo_seed42/config.json
```

**Критерии успеха:**
- ✅ Обучение завершилось без ошибок
- ✅ Модель сохранена
- ✅ Метрики записаны в CSV
- ✅ Чекпоинты созданы
- ✅ Время обучения ~5 минут

**Если ошибка:**
- Проверить логи на stack trace
- Проверить права доступа к директории results/
- Проверить достаточно ли места на диске

### Фаза 3: Тестирование оптимальных параметров

**Цель:** Проверить, что обучение с оптимизированными параметрами достигает награды > 200.

**Команда (CPU):**
```bash
# Запустить PPO с оптимизированными параметрами на CPU
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 500000 \
    --seed 42 \
    --gamma 0.999 \
    --ent-coef 0.01 \
    --gae-lambda 0.98 \
    --n-steps 1024 \
    --n-epochs 4 \
    --device cpu
```

**Ожидаемые результаты:**
- Время обучения: ~10-12 минут
- Средняя награда: > 200
- Стандартное отклонение: < 100
- Сходимость: YES
- Нет GPU предупреждений

**Команда (GPU для сравнения):**
```bash
# Запустить PPO с оптимизированными параметрами на GPU
CUDA_VISIBLE_DEVICES=0 HIP_VISIBLE_DEVICES=0 python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 500000 \
    --seed 42 \
    --gamma 0.999 \
    --ent-coef 0.01 \
    --gae-lambda 0.98 \
    --n-steps 1024 \
    --n-epochs 4 \
    --device auto
```

**Сравнение результатов:**
| Метрика | CPU | GPU | Ожидание |
|---------|-----|-----|----------|
| Время | ~10-12 мин | ~10-12 мин | ~одинаково |
| Награда | > 200 | > 200 | оба успеха |
| Std | < 100 | < 100 | стабильные |
| Предупреждения GPU | Нет | Есть | CPU лучше |

**Критерии успеха:**
- ✅ Обучение завершилось без ошибок
- ✅ Средняя награда > 200
- ✅ Стандартное отклонение < 100
- ✅ Время обучения < 15 минут
- ✅ CPU режим работает без GPU предупреждений

**Если награда < 200:**
- Увеличить timesteps до 1M
- Попробовать другие seed
- Отладить гиперпараметры

### Фаза 4: Тестирование загрузки и инференса

**Цель:** Проверить, что сохраненная модель загружается корректно и предсказывает хорошо.

**Скрипт для тестирования:**
```python
# test_inference.py (создать временный файл или запустить интерактивно)
import gymnasium as gym
from stable_baselines3 import PPO
import numpy as np

# Загрузить модель
model = PPO.load(&quot;results/experiments/ppo_seed42/ppo_seed42_model.zip&quot;)

# Создать среду
env = gym.make(&quot;LunarLander-v3&quot;)

# Запустить 10 эпизодов
rewards = []
for episode in range(10):
    obs, _ = env.reset(seed=episode)
    episode_reward = 0
    done = False
    
    while not done:
        # Детерминированный режим
        action, _ = model.predict(obs, deterministic=True)
        obs, reward, terminated, truncated, info = env.step(action)
        done = terminated or truncated
        episode_reward += reward
    
    rewards.append(episode_reward)
    print(f&quot;Episode {episode + 1}: Reward = {episode_reward:.2f}&quot;)

env.close()

# Статистика
mean_reward = np.mean(rewards)
std_reward = np.std(rewards)
print(f&quot;\n=== Результаты инференса ===&quot;)
print(f&quot;Средняя награда: {mean_reward:.2f} ± {std_reward:.2f}&quot;)
print(f&quot;Минимум: {np.min(rewards):.2f}&quot;)
print(f&quot;Максимум: {np.max(rewards):.2f}&quot;)
print(f&quot;Цель (&gt; 200): {&#x27;✅ Достигнуто&#x27; if mean_reward &gt; 200 else &#x27;❌ Не достигнуто&#x27;}&quot;)
```

**Запуск теста:**
```bash
python -c &quot;... (скопировать скрипт выше)&quot;
# или сохранить как test_inference.py и python test_inference.py
```

**Ожидаемые результаты:**
- Средняя награда: > 200
- Стандартное отклонение: < 100
- Все эпизоды завершаются успешно
- Модель загружается без ошибок

**Критерии успеха:**
- ✅ Модель загружается корректно
- ✅ Предсказания работают
- ✅ Средняя награда > 200 (на 10 эпизодах)
- ✅ Нет ошибок во время инференса

**Если ошибка:**
- Проверить путь к модели
- Проверить модель не повреждена
- Проверить среда корректна

### Фаза 5: Юнит-тесты

**Цель:** Прогнать все unit-тесты и исправить проваленные.

**Команды:**
```bash
# Все unit тесты
pytest tests/unit/ -v --cov=src/ --cov-report=html

# С покрытием
pytest tests/ -v --cov=src/ --cov-report=term-missing

# Конкретный тест
pytest tests/unit/test_seeding.py::test_set_seed_np -v

# Параллельно
pytest tests/unit/ -v -n auto
```

**Ожидаемые результаты:**
- Все тесты passed (603/637 или 100%)
- Coverage > 90%
- HTML отчет в htmlcov/

**Проверки:**
```bash
# Открыть coverage report
open htmlcov/index.html  # или firefox htmlcov/index.html
```

**Критерии успеха:**
- ✅ Все unit тесты проходят
- ✅ Покрытие > 90%
- ✅ Нет критических failed тестов

**Если ошибка:**
- Анализировать traceback: pytest ... -v --tb=long
- Исправить код в src/
- Перепроверить: pytest ... --last-failed

### Фаза 6: Интеграционные тесты

**Цель:** Проверить интеграцию всех компонентов.

**Команды:**
```bash
# Интеграционные тесты
pytest tests/integration/ -v

# С полным пайплайном
pytest tests/integration/test_full_pipeline.py -v

# С фикстурами
pytest tests/integration/ -v --cov=src.experiments
```

**Ожидаемые результаты:**
- Все integration тесты passed
- Артефакты создаются в results/
- Метрики совпадают с ожидаемыми

**Критерии успеха:**
- ✅ Интеграционные тесты проходят
- ✅ Полный пайплайн работает end-to-end
- ✅ Артефакты корректны

**Если ошибка:**
- Проверить конфиги в configs/
- Mock внешние зависимости
- Запустить по одному: pytest ...::test_name

### Фаза 7: Качество кода

**Цель:** Обеспечить высокий quality code.

**Команды:**
```bash
# Linting
ruff check . --fix

# Formatting
ruff format .

# Imports
ruff check --select I . --fix

# Stats
ruff stats .

# Type checking
mypy src/ tests/ --strict

# Pre-commit
pre-commit run --all-files
```

**Ожидаемые результаты:**
- ruff check: 0 issues
- mypy: No errors
- Black formatted

**Критерии успеха:**
- ✅ ruff check проходит без ошибок
- ✅ mypy strict проходит
- ✅ Docstrings в Google style

**Если ошибка:**
- ruff check --fix для auto-fix
- Добавить type hints
- Обновить docstrings

### Фаза 8: Производительность

**Цель:** Замерить и оптимизировать производительность.

**Команды:**
```bash
# Время на 50K steps (CPU)
time python -m src.experiments.completion.baseline_training --algo ppo --timesteps 50000 --seed 42 --device cpu

# Время на 500K steps (GPU)
time CUDA_VISIBLE_DEVICES=0 HIP_VISIBLE_DEVICES=0 python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --device auto

# Мониторинг памяти (в другом терминале)
htop
# или watch -n 1 free -h
```

**Ожидаемые результаты:**
| Benchmark | CPU | GPU |
|-----------|-----|-----|
| 50K steps | <1 мин | <1 мин |
| 500K steps | <15 мин | <12 мин |
| Память max | <2GB | <3GB |

**Критерии успеха:**
- ✅ Время <15 мин для 500K
- ✅ Нет утечек памяти
- ✅ CPU/GPU сбалансировано

**Если медленно:**
- Уменьшить n_epochs или batch_size
- Проверить torch.backends.cudnn.benchmark = False

### Фаза 9: Воспроизводимость

**Цель:** Обеспечить reproducibility результатов.

**Команды:**
```bash
# Run 1 seed=42
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000 --seed 42 --exp-name ppo_seed42_run1

# Run 2 seed=42
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000 --seed 42 --exp-name ppo_seed42_run2

# Разные seeds
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000 --seed 123 --exp-name ppo_seed123
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000 --seed 999 --exp-name ppo_seed999
```

**Проверки:**
```bash
# Сравнить метрики (последние 10 строк)
tail -10 results/experiments/ppo_seed42_run1/metrics.csv
tail -10 results/experiments/ppo_seed42_run2/metrics.csv
diff <(tail -10 results/experiments/ppo_seed42_run1/metrics.csv) <(tail -10 results/experiments/ppo_seed42_run2/metrics.csv)
```

**Критерии успеха:**
- ✅ Run1 == Run2 (идентичные метрики)
- ✅ Разные seeds дают разные траектории
- ✅ Seed влияет на reward variance

**Если не воспроизводимо:**
- Проверить set_seed() вызывается первым
- torch.backends.cudnn.deterministic = True
- np.random.seed, random.seed

### Фаза 10: Отладка и исправление

**Цель:** Собрать и исправить все баги.

**Шаги:**
1. Собрать список багов из всех фаз
2. Создать таблицу:

| Баг | Приоритет | Файл | Описание | Статус |
|-----|-----------|------|----------|--------|
| GPU warning | High | trainer.py | Предупреждение ROCm | Fixed |
| Test fail | Critical | test_seeding.py | Seed mismatch | Fixed |

3. Исправить по приоритету
4. После каждого fix: ruff check, pytest

**Команды для отладки:**
```bash
# Debug с pdb
python -m pdb src/experiments/completion/baseline_training.py ...

# Verbose logging
python -m src.experiments.completion.baseline_training ... --verbose 1
```

**Критерии успеха:**
- ✅ Нет critical/high багов
- ✅ Все фазы 1-9 проходят
- ✅ Тесты 100%

**Процесс:**
- Edit files with ruff --fix
- Commit после каждой фазы

### Фаза 11: Оптимизация параметров (если нужно)

**Цель:** Достичь лучших результатов если базовые не идеальны.

**Анализ:**
- Если mean reward < 200 или std > 100

**Эксперименты:**
```bash
# Learning rate grid
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --learning-rate 3e-4 --exp-name lr_3e4

python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --learning-rate 1e-4 --exp-name lr_1e4

# ent_coef
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --ent-coef 0.005 --exp-name ent_0.005

# n_steps / n_epochs
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --n-steps 2048 --n-epochs 8 --exp-name large_batch
```

**Критерии успеха:**
- ✅ Финальная reward > 200 stable
- ✅ Лучшие params зафиксированы в config.yaml
- ✅ Или подтвердить текущие оптимальны

**Если не улучшается:**
- Принять текущие
- Документировать в отчете

### Фаза 12: Документация

**Цель:** Обновить всю документацию.

**Задачи:**
```bash
# Обновить README.md с новыми командами
# Добавить раздел QUICKSTART

# Обновить TROUBLESHOOTING.md с новыми issues
# PROJECT_CONTEXT.md с статусом &quot;COMPLETED&quot;

# Создать QUICKSTART.md если нет
echo &quot;# Quickstart\nconda activate rocm\npython -m src...&quot; > QUICKSTART.md
```

**Проверки:**
- Все ссылки работают: markdown-link-check README.md
- Docstrings: ruff check --select D

**Критерии успеха:**
- ✅ README updated
- ✅ Troubleshooting complete
- ✅ All docs consistent

### Фаза 13: Финальная проверка

**Цель:** Полная верификация с нуля.

**Команды:**
```bash
# Clean
rm -rf results/experiments/

# Full pipeline
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --device cpu

# All tests
pytest tests/ -v --cov

# Quality
ruff check . && mypy src/

# Generate plots
python -m src.visualization.plots.generate_all --log-dir results/logs/
```

**Ожидаемые артефакты:**
- Модель .zip
- metrics.csv (tail >200)
- Видео .mp4
- Plots .png

**Критерии успеха:**
- ✅ Всё работает с нуля
- ✅ Все чекпоинты [ ]
- ✅ Финальный отчет готов

## 4. Таблица прогресса

## Таблица прогресса

| Фаза | Название | Статус | Начало | Конец | Заметки |
|------|----------|--------|--------|-------|---------|
| 1 | Верификация окружения | 🟡 В процессе | - | - | Проверить зависимости |
| 2 | Тестирование базового пайплайна | ⚪ Не начато | - | - | |
| 3 | Тестирование оптимальных параметров | ⚪ Не начато | - | - | |
| 4 | Тестирование загрузки и инференса | ⚪ Не начато | - | - | |
| 5 | Юнит-тесты | ⚪ Не начато | - | - | 603/637 прошли |
| 6 | Интеграционные тесты | ⚪ Не начато | - | - | |
| 7 | Качество кода | ⚪ Не начато | - | - | ruff прошел |
| 8 | Производительность | ⚪ Не начато | - | - | |
| 9 | Воспроизводимость | ⚪ Не начато | - | - | |
| 10 | Отладка и исправление | ⚪ Не начато | - | - | |
| 11 | Оптимизация параметров | ⚪ Не начато | - | - | |
| 12 | Документация | ⚪ Не начато | - | - | 2 отчета созданы |
| 13 | Финальная проверка | ⚪ Не начато | - | - | |

**Легенда:**
- 🟢 Завершено
- 🟡 В процессе
- 🟠 Есть проблемы
- 🔴 Провалено
- ⚪ Не начато

## 5. Сводка команд

## Быстрый справочник команд

### Окружение
```bash
# Активировать environment
conda activate rocm

# Проверить версии
python --version
pip list | grep -E &quot;torch|stable-baselines3|gymnasium&quot;
```

### Обучение
```bash
# Базовый тест (200K, default)
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000 --seed 42

# Оптимальный (500K, optimized, CPU)
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --n-steps 1024 --n-epochs 4 --device cpu

# Оптимальный (500K, optimized, GPU)
CUDA_VISIBLE_DEVICES=0 HIP_VISIBLE_DEVICES=0 python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --n-steps 1024 --n-epochs 4 --device auto
```

### Тестирование
```bash
# Unit tests
pytest tests/unit/ -v

# Integration tests
pytest tests/integration/ -v

# С покрытием
pytest tests/ -v --cov=src/ --cov-report=html
```

### Качество кода
```bash
# Linting
ruff check . --fix

# Форматирование
ruff format .

# Статистика
ruff stats .
```

### Проверка артефактов
```bash
# Все файлы эксперимента
ls -lh results/experiments/ppo_seed42/

# Метрики
head -20 results/experiments/ppo_seed42/metrics.csv
tail -10 results/experiments/ppo_seed42/eval_log.csv

# Конфигурация
cat results/experiments/ppo_seed42/config.json

# Графики и видео
ls -lh results/experiments/ppo_seed42/*.png results/experiments/ppo_seed42/*.mp4
```

## 6. Критерии успеха проекта

## Критерии успеха проекта

### Обязательные (Must Have)
- ✅ Агент достигает средней награды > 200
- ✅ Обучение завершается без ошибок
- ✅ Модель сохраняется и загружается корректно
- ✅ Метрики записываются в CSV
- ✅ Все unit tests проходят (>95%)
- ✅ Код соответствует PEP 8
- ✅ Документация полная

### Желательные (Should Have)
- ✅ Время обучения < 15 минут
- ✅ Стандартное отклонение < 100
- ✅ CPU обучение работает без предупреждений
- ✅ Результаты воспроизводимы
- ✅ Визуализации создаются
- ✅ Видео демонстрации работает

### Опциональные (Nice to Have)
- ⏳ Интеграционные тесты проходят
- ⏳ Покрытие тестами >80%
- ⏳ Время обучения < 5 минут
- ⏳ Средняя награда > 250
- ⏳ Type checking (mypy) проходит

## 7. План действий в случае проблем

## План действий в случае проблем

### Обучение не сходится (< 150 награда)

**Возможные причины:**
1. Неправильные гиперпараметры
2. Недостаточно timesteps
3. Проблемы с окружением

**Решения:**
```bash
# 1. Увеличить timesteps до 1M
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 1000000 --seed 42 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --n-steps 1024 --n-epochs 4 --device cpu

# 2. Попробовать другой seed
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 123 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --n-steps 1024 --n-epochs 4 --device cpu

# 3. Попробовать A2C алгоритм
python -m src.experiments.completion.baseline_training --algo a2c --timesteps 500000 --seed 42 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --device cpu
```

### GPU предупреждения появляются

**Возможная причина:** Переменная HIP_VISIBLE_DEVICES не установлена

**Решение:**
```bash
# Установить обе переменные для ROCm
CUDA_VISIBLE_DEVICES=&quot;&quot; HIP_VISIBLE_DEVICES=&quot;&quot; python -m src.experiments.completion.baseline_training --algo ppo --timesteps 500000 --seed 42 --gamma 0.999 --ent-coef 0.01 --gae-lambda 0.98 --n-steps 1024 --n-epochs 4 --device cpu
```

### Тесты проваливаются

**Возможные причины:**
1. Устаревшие функции в тестах
2. Отсутствующие модули
3. Изменения в API

**Решения:**
```bash
# 1. Запустить конкретный тест с verbose выводом
pytest tests/unit/test_a2c_agent.py::TestA2CAgent::test_init_success -v --tb=short

# 2. Запустить все unit tests кроме проблемных
pytest tests/unit/ -v --ignore=tests/unit/test_a2c_agent.py --ignore=tests/unit/test_td3_agent.py

# 3. Игнорировать предупреждения
pytest tests/unit/ -v -W ignore::DeprecationWarning
```

### Модель не загружается

**Возможные причины:**
1. Файл поврежден
2. Несовместимость версий SB3
3. Неверный путь

**Решения:**
```bash
# 1. Проверить файл существует
ls -lh results/experiments/ppo_seed42/ppo_seed42_model.zip

# 2. Проверить файл не поврежден
unzip -t results/experiments/ppo_seed42/ppo_seed42_model.zip

# 3. Попробовать загрузить с verbose
python -c &quot;from stable_baselines3 import PPO; model = PPO.load(&#x27;results/experiments/ppo_seed42/ppo_seed42_model.zip&#x27;, verbose=1); print(f&#x27;Устройство: {model.device}&#x27;)&quot;
```

### Нет места на диске

**Решение:**
```bash
# Проверить место
df -h results/

# Очистить старые эксперименты
rm -rf results/experiments/*/checkpoints/
rm -rf results/experiments/*_model.zip

# Очистить кэш Python
find . -type d -name __pycache__ -exec rm -rf {} + 2&gt;/dev/null
```

## 8. Приложение

## Приложение

### А. Список всех файлов проекта
```
training_an_agent_in_a_classical_environment/
├── src/
│   ├── experiments/
│   │   └── completion/
│   │       ├── baseline_training.py        # Основной скрипт обучения
│   │       └── config.py                # Конфигурации
│   ├── training/
│   │   ├── callbacks.py                # Callbacks (checkpoint, eval, metrics)
│   │   ├── evaluation.py               # Оценка модели
│   │   └── results.py                 # Результаты обучения
│   ├── utils/
│   │   └── seeding.py                 # Установка seed
│   └── agents/
│       ├── a2c_agent.py
│       ├── ppo_agent.py
│       └── td3_agent.py
├── tests/
│   ├── unit/
│   ├── integration/
│   └── ...
├── results/
│   └── experiments/
│       └── ppo_seed42/                # Артефакты эксперимента
├── scripts/
│   └── ...
├── docs/
│   ├── QUICKSTART.md
│   ├── TROUBLESHOOTING.md
│   └── ...
├── CPU_vs_GPU_Comparison.md            # Отчет о сравнении CPU vs GPU
├── PROJECT_COMPLETION_REPORT.md        # Финальный отчет о проекте
├── 004-test-and-fix-experiments.md    # Этот план
└── requirements.txt
```

### Б. Полезные ссылки
- Stable-Baselines3 Documentation: https://stable-baselines3.readthedocs.io/
- Gymnasium Documentation: https://gymnasium.farama.org/
- LunarLander-v3: https://gymnasium.farama.org/environments/box2d/lunar_lander/
- ROCm Documentation: https://rocm.docs.amd.com/

### В. Контакт и поддержка
- Если есть вопросы, смотрите TROUBLESHOOTING.md
- Для багов, проверьте логи в results/experiments/*/logs.txt
- Для анализа, смотрите PROJECT_COMPLETION_REPORT.md
