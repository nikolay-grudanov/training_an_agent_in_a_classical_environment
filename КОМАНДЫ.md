# 📋 Полный справочник команд проекта

## Проект: RL Experiments Completion & Convergence
**Статус:** ✅ РЕАЛИЗОВАН  
**Ветка:** `003-experiments-completion`  
**Дата:** 15 января 2026 г.

---

## ⚡ Быстрый старт (2 способа)

### Способ 1: TUI интерфейс (рекомендуется) 🖥️

```bash
# Запуск интерактивного меню
python run_experiments.py

# Проверка зависимостей
python run_experiments.py --check-deps
```

**Доступные меню:**
- 🎓 Обучение моделей (PPO/A2C)
- 📊 Оценка моделей
- 📈 Генерация графиков
- 🎬 Генерация видео
- 📋 Генерация отчётов
- 🧪 Тестирование
- ✅ Проверка качества кода
- 🚀 Полный workflow (автоматически выполняет все шаги)

### Способ 2: Прямые команды

```bash
# 1. Обучение PPO агента (200K шагов)
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 200000

# 2. Обучение A2C агента (200K шагов)
python -m src.experiments.completion.baseline_training --algo a2c --timesteps 200000

# 3. Запуск gamma эксперимента
python -m src.experiments.completion.gamma_experiment --gamma 0.90 0.99 0.999

# 4. Генерация графиков
python -m src.visualization.graphs --experiment ppo_seed42 --type learning_curve

# 5. Генерация видео
python -m src.visualization.video --model results/experiments/ppo_seed42/ppo_seed42_model.zip --output video.mp4
```

---

## 📚 Полный справочник

### 1. ОБУЧЕНИЕ БАЗОВЫХ МОДЕЛЕЙ (User Story 1)

#### 1.1 Обучение PPO агента

**Стандартное обучение (200K шагов):**
```bash
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 200000 \
    --seed 42 \
    --gamma 0.99
```

**Быстрое обучение для тестирования (50K шагов):**
```bash
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 50000 \
    --seed 42
```

**Расширенное обучение (300K шагов):**
```bash
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 300000 \
    --seed 42
```

#### 1.2 Обучение A2C агента

**Стандартное обучение (200K шагов):**
```bash
python -m src.experiments.completion.baseline_training \
    --algo a2c \
    --timesteps 200000 \
    --seed 42 \
    --gamma 0.99
```

**Быстрое обучение:**
```bash
python -m src.experiments.completion.baseline_training \
    --algo a2c \
    --timesteps 50000 \
    --seed 42
```

#### 1.3 Продолжение обучения с чекпоинта

```bash
python -m src.experiments.completion.baseline_training \
    --algo ppo \
    --timesteps 200000 \
    --seed 42 \
    --resume-from results/experiments/ppo_seed42/checkpoints/checkpoint_100000
```

---

### 2. ОЦЕНКА ОБУЧЕННЫХ МОДЕЛЕЙ

#### 2.1 Оценка одной модели

```bash
python -c "
from src.training.evaluation import evaluate_agent

result = evaluate_agent(
    model_path='results/experiments/ppo_seed42/ppo_seed42_model.zip',
    env_id='LunarLander-v3',
    n_eval_episodes=10,
)
print(f'Средняя награда: {result[\"mean_reward\"]:.2f} ± {result[\"std_reward\"]:.2f}')
print(f'Сходимость: {\"ДА\" if result[\"convergence_achieved\"] else \"НЕТ\"}')
"
```

#### 2.2 Оценка всех базовых моделей

```bash
python -c "
from src.training.evaluation import evaluate_agent

for exp_id in ['a2c_seed42', 'ppo_seed42']:
    result = evaluate_agent(f'results/experiments/{exp_id}/{exp_id}_model.zip')
    print(f'{exp_id}: {result[\"mean_reward\"]:.2f} ± {result[\"std_reward\"]:.2f}')
"
```

---

### 3. ГЕНЕРАЦИЯ ГРАФИКОВ (User Story 2)

#### 3.1 График обучения для одного эксперимента

```bash
python -m src.visualization.graphs \
    --experiment a2c_seed42 \
    --type learning_curve \
    --output results/experiments/a2c_seed42/reward_curve.png \
    --title "Кривая обучения A2C (Seed=42)"
```

#### 3.2 График для PPO

```bash
python -m src.visualization.graphs \
    --experiment ppo_seed42 \
    --type learning_curve \
    --output results/experiments/ppo_seed42/reward_curve.png \
    --title "Кривая обучения PPO (Seed=42)"
```

#### 3.3 Сравнительный график (A2C vs PPO)

```bash
python -m src.visualization.graphs \
    --experiment a2c_seed42,ppo_seed42 \
    --type comparison \
    --output results/comparison/a2c_vs_ppo.png \
    --title "Сравнение алгоритмов: A2C vs PPO"
```

#### 3.4 График сравнения gamma

```bash
python -m src.visualization.graphs \
    --experiment gamma_090,gamma_099,gamma_0999 \
    --type gamma_comparison \
    --output results/comparison/gamma_comparison.png \
    --title "Сравнение гиперпараметра gamma"
```

#### 3.5 Типы графиков

| Тип | Описание | Выходной файл |
|-----|----------|---------------|
| `learning_curve` | График прогресса обучения | `*_seed*/reward_curve.png` |
| `comparison` | Сравнение 2+ экспериментов | `comparison/*.png` |
| `gamma_comparison` | Сравнение вариаций gamma | `comparison/gamma_*.png` |

---

### 4. ГЕНЕРАЦИЯ ВИДЕО (User Story 3)

#### 4.1 Видео для модели A2C

```bash
python -m src.visualization.video \
    --model results/experiments/a2c_seed42/a2c_seed42_model.zip \
    --output results/experiments/a2c_seed42/video.mp4 \
    --episodes 5 \
    --fps 30
```

#### 4.2 Видео для модели PPO

```bash
python -m src.visualization.video \
    --model results/experiments/ppo_seed42/ppo_seed42_model.zip \
    --output results/experiments/ppo_seed42/video.mp4 \
    --episodes 5 \
    --fps 30
```

#### 4.3 Видео с расширенными настройками

```bash
python -m src.visualization.video \
    --model results/experiments/ppo_seed42/ppo_seed42_model.zip \
    --output results/videos/ppo_demo.mp4 \
    --episodes 10 \
    --fps 30 \
    --show-scores
```

#### 4.4 Параметры видео

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--model` | Обязательно | Путь к модели (.zip) |
| `--output` | Обязательно | Путь к видео (.mp4) |
| `--episodes` | 5 | Количество эпизодов |
| `--fps` | 30 | Кадров в секунду |
| `--show-scores` | Нет | Показывать очки эпизодов |

---

### 5. GAMMA ЭКСПЕРИМЕНТ (User Story 4)

#### 5.1 Стандартный эксперимент (0.90, 0.99, 0.999)

```bash
python -m src.experiments.completion.gamma_experiment \
    --gamma 0.90 0.99 0.999 \
    --timesteps 100000 \
    --seed 42
```

#### 5.2 Эксперимент с расширенным обучением

```bash
python -m src.experiments.completion.gamma_experiment \
    --gamma 0.90 0.99 0.999 \
    --timesteps 200000 \
    --seed 42
```

#### 5.3 Эксперимент с пользовательскими значениями

```bash
python -m src.experiments.completion.gamma_experiment \
    --gamma 0.95 0.99 0.995 \
    --timesteps 100000 \
    --seed 42
```

---

### 6. ГЕНЕРАЦИЯ ОТЧЁТОВ (User Story 5)

#### 6.1 Отчёт по базовым экспериментам

```bash
python -m src.reporting.report_generator \
    --output results/reports/experiment_report.md \
    --experiments a2c_seed42 ppo_seed42 \
    --include-graphs \
    --include-videos
```

#### 6.2 Полный отчёт с gamma экспериментом

```bash
python -m src.reporting.report_generator \
    --output results/reports/full_report.md \
    --experiments a2c_seed42 ppo_seed42 gamma_090 gamma_099 gamma_0999 \
    --include-graphs \
    --include-videos
```

#### 6.3 Краткий отчёт без медиа

```bash
python -m src.reporting.report_generator \
    --output results/reports/quick_report.md \
    --experiments a2c_seed42 ppo_seed42 \
    --no-include-graphs \
    --no-include-videos
```

#### 6.4 Параметры отчёта

| Параметр | Описание |
|----------|----------|
| `--output` | Путь к файлу markdown |
| `--experiments` | Список ID экспериментов |
| `--include-graphs` | Включить ссылки на графики |
| `--include-videos` | Включить ссылки на видео |

---

### 7. ТЕСТИРОВАНИЕ

#### 7.1 Все unit-тесты

```bash
pytest tests/unit/ -v --cov=src/
```

#### 7.2 Тесты конкретных модулей

```bash
# Тесты колбэков
pytest tests/unit/test_callbacks.py -v

# Тесты оценки
pytest tests/unit/test_evaluation.py -v

# Тесты графиков
pytest tests/unit/test_graphs.py -v

# Тесты статистики
pytest tests/unit/test_statistics.py -v
```

#### 7.3 Интеграционные тесты

```bash
# Интеграция обучения
pytest tests/integration/test_baseline_training.py -v

# Интеграция видео
pytest tests/integration/test_video.py -v

# Полный пайплайн
pytest tests/integration/test_full_pipeline.py -v
```

#### 7.4 Все тесты с покрытием

```bash
pytest tests/ -v --cov=src/ --cov-report=html
```

---

### 8. ПРОВЕРКА КАЧЕСТВА КОДА

#### 8.1 Проверка стиля кода

```bash
# Проверка ошибок
ruff check .

# Автоисправление ошибок
ruff check . --fix

# Форматирование кода
ruff format .
```

#### 8.2 Проверка типов

```bash
mypy src/ --strict
```

#### 8.3 Полная проверка качества

```bash
ruff check . --fix && ruff format . && mypy src/ --strict
```

---

## 📊 Параметры команд

### `python -m src.experiments.completion.baseline_training`

| Параметр | Значение | По умолчанию | Описание |
|----------|----------|--------------|----------|
| `--algo` | `a2c`, `ppo` | `ppo` | Алгоритм обучения |
| `--timesteps` | число | `200000` | Количество шагов |
| `--seed` | число | `42` | Seed для воспроизводимости |
| `--gamma` | число | `0.99` | Discount factor |
| `--checkpoint-freq` | число | `50000` | Частота чекпоинтов |
| `--resume-from` | путь | - | Продолжить с чекпоинта |

### `python -m src.experiments.completion.gamma_experiment`

| Параметр | Значение | По умолчанию | Описание |
|----------|----------|--------------|----------|
| `--gamma` | числа | `0.90 0.99 0.999` | Значения gamma |
| `--timesteps` | число | `100000` | Шагов на конфигурацию |
| `--seed` | число | `42` | Seed для воспроизводимости |

### `python -m src.visualization.graphs`

| Параметр | Значение | По умолчанию | Описание |
|----------|----------|--------------|----------|
| `--experiment` | ID | Обязательно | ID эксперимента |
| `--type` | learning_curve/comparison/gamma_comparison | learning_curve | Тип графика |
| `--output` | путь | Обязательно | Выходной файл |
| `--title` | текст | - | Заголовок графика |

### `python -m src.visualization.video`

| Параметр | Значение | По умолчанию | Описание |
|----------|----------|--------------|----------|
| `--model` | путь | Обязательно | Путь к модели |
| `--output` | путь | Обязательно | Выходной файл |
| `--episodes` | число | `5` | Количество эпизодов |
| `--fps` | число | `30` | Кадров в секунду |

### `python -m src.reporting.report_generator`

| Параметр | Значение | По умолчанию | Описание |
|----------|----------|--------------|----------|
| `--output` | путь | Обязательно | Выходной файл |
| `--experiments` | ID | Обязательно | Список экспериментов |
| `--include-graphs` | флаг | True | Включить графики |
| `--include-videos` | флаг | True | Включить видео |

---

## 📁 Выходные файлы

### После обучения PPO

```
results/
├── experiments/
│   └── ppo_seed42/
│       ├── ppo_seed42_model.zip          # Обученная модель
│       ├── config.json                   # Конфигурация
│       ├── metrics.csv                   # Метрики обучения
│       ├── eval_log.csv                  # Логи оценки
│       ├── reward_curve.png              # График обучения
│       ├── video.mp4                     # Демонстрационное видео
│       └── checkpoints/
│           ├── checkpoint_50000.zip
│           ├── checkpoint_100000.zip
│           └── checkpoint_150000.zip
```

### После gamma эксперимента

```
results/
├── experiments/
│   └── gamma_experiments/
│       ├── gamma_090/
│       │   ├── gamma_090_model.zip
│       │   └── metrics.csv
│       ├── gamma_099/
│       │   └── ...
│       └── gamma_0999/
│           └── ...
└── comparison/
    ├── a2c_vs_ppo.png
    └── gamma_comparison.png
```

### После генерации отчёта

```
results/
└── reports/
    └── experiment_report.md              # Сгенерированный отчёт
```

---

## 🔍 Пример использования: Полный workflow

### Способ 1: Автоматический (через TUI) 🚀

```bash
# Активация окружения
conda activate rocm

# Запуск полного workflow автоматически
python run_experiments.py
# Выберите опцию 8: 🚀 ПОЛНЫЙ WORKFLOW
```

**Автоматический workflow включает:**
1. Обучение PPO (200K шагов)
2. Обучение A2C (200K шагов)
3. Генерация сравнительного графика
4. Генерация видео PPO агента
5. Генерация итогового отчёта

### Способ 2: Пошаговый (через TUI) 📋

```bash
python run_experiments.py
```

**Шаги:**
1. Выберите `1` → `1` (Обучение PPO - 200K)
2. Выберите `1` → `5` (Обучение A2C - 200K)
3. Выберите `3` → `3` (Сравнение PPO vs A2C)
4. Выберите `4` → `1` (Видео PPO агента)
5. Выберите `5` → `1` (Отчёт по базовым моделям)
6. Выберите `7` → `5` (Полная проверка качества)

### Способ 3: Прямые команды (вручную) 💻

```bash
# 1. Активация окружения
conda activate rocm

# 2. Обучение PPO
python -m src.experiments.completion.baseline_training \
    --algo ppo --timesteps 200000 --seed 42
echo "✓ PPO обучен"

# 3. Обучение A2C
python -m src.experiments.completion.baseline_training \
    --algo a2c --timesteps 200000 --seed 42
echo "✓ A2C обучен"

# 4. Gamma эксперимент
python -m src.experiments.completion.gamma_experiment \
    --gamma 0.90 0.99 0.999 --timesteps 100000 --seed 42
echo "✓ Gamma эксперимент завершён"

# 5. Генерация графиков
python -m src.visualization.graphs \
    --experiment a2c_seed42,ppo_seed42 \
    --type comparison \
    --output results/comparison/a2c_vs_ppo.png
echo "✓ Графики созданы"

# 6. Генерация видео
python -m src.visualization.video \
    --model results/experiments/ppo_seed42/ppo_seed42_model.zip \
    --output results/experiments/ppo_seed42/video.mp4 \
    --episodes 5
echo "✓ Видео создано"

# 7. Генерация отчёта
python -m src.reporting.report_generator \
    --output results/reports/experiment_report.md \
    --experiments a2c_seed42 ppo_seed42 gamma_099 \
    --include-graphs --include-videos
echo "✓ Отчёт сгенерирован"

# 8. Запуск тестов
pytest tests/unit/ -v --cov=src/
echo "✓ Все тесты пройдены!"

echo ""
echo "🎉 Полный workflow выполнен!"
```

---

## ⚠️ Частые ошибки

### Ошибка: Модель не достигает сходимости

**Причина:** Недостаточное количество шагов или неправильный seed

**Решение:**
```bash
# Проверить seed
python -c "from src.utils.seeding import set_seed; set_seed(42)"

# Увеличить количество шагов
python -m src.experiments.completion.baseline_training --algo ppo --timesteps 300000
```

### Ошибка: Генерация видео не работает

**Причина:** Недостаточно памяти или неправильный режим рендеринга

**Решение:**
```bash
# Проверить память
free -h

# Проверить наличие модели
ls -la results/experiments/*/*_model.zip

# Использовать меньшее разрешение
python -m src.visualization.video --model ... --width 400 --height 300
```

### Ошибка: Оценка возвращает NaN

**Причина:** Модель загружена без окружения

**Решение:**
```bash
# Перезагрузить модель с окружением
python -c "
from stable_baselines3 import PPO
import gymnasium as gym

model = PPO.load('results/experiments/ppo_seed42/ppo_seed42_model.zip')
env = gym.make('LunarLander-v3')
"
```

---

## 📈 Ожидаемые результаты

### Цели сходимости

| Алгоритм | Целевая награда | Время (CPU) |
|----------|-----------------|-------------|
| A2C | ≥200 | ~15-20 минут |
| PPO | ≥200 | ~25-30 минут |

### Гипотеза gamma эксперимента

**Гипотеза:** gamma=0.99 обеспечивает лучший баланс

| Gamma | Поведение | Ожидаемая награда |
|-------|-----------|-------------------|
| 0.90 | Быстрое начальное обучение, ниже финальная | 180-200 |
| 0.99 | Сбалансированный, хорошая производительность | 220-260 |
| 0.999 | Медленное начальное, стабильная посадка | 200-240 |

---

## 📞 Документация

- **Спецификация:** `specs/003-experiments-completion/spec.md`
- **План:** `specs/003-experiments-completion/plan.md`
- **Исследование:** `specs/003-experiments-completion/research.md`
- **Модель данных:** `specs/003-experiments-completion/data-model.md`
- **Задачи:** `specs/003-experiments-completion/tasks.md`
- **Квикстарт:** `specs/003-experiments-completion/quickstart.md`
- **Конституция:** `.specify/memory/constitution.md`

---

**Проект готов к использованию! ✅**

---

## 🖥️ TUI Интерфейс (Интерактивное меню)

Новый `run_experiments.py` предоставляет удобный интерфейс для управления всеми экспериментами.

### Возможности TUI

| Категория | Функции |
|-----------|---------|
| 🎓 Обучение | PPO/A2C с разными количествами шагов, custom ввод |
| 📊 Оценка | Оценка отдельных моделей или обеих сразу |
| 📈 Графики | Learning curves, comparison, gamma comparison |
| 🎬 Видео | Генерация видео для моделей, custom настройки |
| 📋 Отчёты | Baseline, full, quick reports |
| 🧪 Тесты | Unit, integration, coverage tests |
| ✅ Качество кода | Ruff check/fix, format, mypy |
| 🚀 Workflow | Полный автоматический pipeline |

### Быстрая проверка зависимостей

```bash
python run_experiments.py --check-deps
```

### Горячие клавиши TUI

- `0` - Назад/Выход
- `8` - Полный workflow (мини-кнопка для быстрого запуска)

### Пример использования TUI

```bash
# Запуск TUI
python run_experiments.py

# Интерактивный выбор:
# → 1 → 1  (Обучение PPO 200K)
# → 1 → 8  (Custom обучение: введите algo, timesteps, seed, gamma)
# → 8     (Полный workflow)
```
