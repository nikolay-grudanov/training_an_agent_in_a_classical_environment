# 📁 Структура проекта RL Agent Training System

## Обзор проекта 🚀

**Название проекта:** RL Agent Training System  
**Среда:** LunarLander-v3 (Gymnasium)  
**Статус:** Фаза 7 завершена (Финальная полировка)  
**Ветка:** 001-cleanup-ppo-a2c-experiments  
**Язык:** Python 3.10.14  

**Ключевые технологии:**
- Stable-Baselines3 (PPO, A2C, SAC, TD3)
- Gymnasium (LunarLander-v3)
- PyTorch с поддержкой ROCm
- NumPy, Matplotlib, imageio
- pytest, ruff, mypy

Проект представляет собой production-ready систему для обучения агентов подкрепляющего обучения (RL) в классической среде LunarLander-v3. Система обеспечивает:
- **Модульность:** Четкое разделение обязанностей
- **Воспроизводимость:** Глобальные seed'ы и детерминизм
- **Тестирование:** 200+ unit-тестов + integration-тесты
- **Типизация:** 100% type hints (mypy strict)
- **Качество кода:** 100% соответствие Ruff/Black

[Руководство по AGENTS.md →](#agentsmd---руководство-по-разработке-агентов)

## 1. Дерево структуры проекта 📊

```
.
├── README.md                    # Основная документация проекта
├── AGENTS.md                    # Руководство для агентов (в разработке)
├── pyproject.toml               # Конфигурация инструментов (ruff, pytest, etc.)
├── .gitignore                   # Игнорируемые файлы (results/, __pycache__/)
├── src/                         # Исходный код 📂
│   ├── __init__.py              # Инициализация пакета
│   ├── agents/                  # Реализации RL агентов
│   │   ├── __init__.py
│   │   ├── base.py              # Базовый класс Agent
│   │   ├── ppo_agent.py         # PPO агент (845 строк)
│   │   ├── a2c_agent.py         # A2C агент
│   │   ├── sac_agent.py         # SAC агент
│   │   └── td3_agent.py         # TD3 агент
│   ├── training/                # Пайплайн обучения (13 файлов)
│   │   ├── __init__.py
│   │   ├── trainer.py           # Главный оркестратор (1223 строки)
│   │   ├── train.py             # CLI интерфейс
│   │   ├── callbacks.py         # Кастомные callbacks
│   │   ├── checkpoint.py        # Управление чекпоинтами
│   │   ├── evaluation.py        # Оценка агентов
│   │   └── ... (10+ файлов)
│   ├── experiments/             # Управление экспериментами
│   │   └── completion/          # Базовые и gamma эксперименты
│   │       ├── baseline_training.py
│   │       └── gamma_experiments.py
│   ├── visualization/           # Визуализация
│   │   ├── graphs.py            # Графики метрик
│   │   └── video.py             # Генерация видео
│   ├── audit/                   # Система аудита кода
│   ├── cleanup/                 # Система очистки проекта
│   ├── evaluation/              # Оценка агентов
│   ├── reporting/               # Генерация отчетов
│   └── utils/                   # Утилиты (8 файлов)
│       ├── __init__.py
│       ├── seeding.py           # Воспроизводимость (190 строк)
│       ├── metrics_exporter.py  # Экспорт метрик
│       └── ... (6+ файлов)
├── tests/                       # Тесты 📂
│   ├── unit/                    # Unit-тесты (200+)
│   │   ├── test_seeding.py      # Тесты seeding (152 строки)
│   │   └── ... (199+ файлов)
│   └── integration/             # Integration-тесты (6 файлов)
│       └── test_trainer.py
├── specs/                       # Спецификации
│   ├── 001/                     # Исходная спецификация
│   │   ├── spec.md
│   │   ├── plan.md
│   │   └── tasks.md
│   ├── 002/                     # Cleanup & validation
│   │   └── ...
│   └── 003/                     # Experiments completion
│       └── ...
├── configs/                     # Конфигурации YAML
│   ├── training_schema.yaml
│   └── experiment_schema.yaml
├── results/                     # Результаты экспериментов (gitignore)
│   ├── experiments/
│   ├── logs/
│   ├── metrics/                 # JSON метрики
│   ├── models/                  # .zip модели
│   ├── videos/                  # MP4 видео
│   └── reports/                 # Генерированные отчеты
├── pytest.ini                   # Конфигурация тестов
└── requirements.txt             # Зависимости
```

*Примечание: `results/` игнорируется в git. Полная структура генерируется `tree -L 3`.*
[Подробные описания директорий →](#2-описания-директорий)

## 2. Описания директорий 📋

### 📁 Root Directory (Корневая директория)

**Назначение:** Точка входа проекта, конфигурации, документация.

**Ключевые файлы:**
| Файл | Назначение |
|------|------------|
| `README.md` | Обзор проекта, quick start |
| `AGENTS.md` | [Руководство по агентам →](#agentsmd---руководство-по-разработке-агентов) |
| `pyproject.toml` | Ruff, pytest, mypy конфиг |
| `pytest.ini` | Настройки тестов |
| `.gitignore` | Игнор results/, pycache/ |

**Зависимости:** Нет (внешние).

### 📁 `src/` - Исходный код

**Назначение:** Основная логика RL обучения.

**Поддиректории:**
- `agents/` - Агенты
- `training/` - Обучение
- `utils/` - Утилиты

**Зависимости:** stable-baselines3, gymnasium, torch.

#### `src/agents/` 

**Назначение:** Реализации RL агентов (PPO, A2C и др.).

**Ключевые файлы:**
- `base.py`: Базовый `Agent` и `AgentConfig`.
- `ppo_agent.py`: PPO с оптимизацией для LunarLander (845 строк).

**Зависимости:** `src.utils`, stable-baselines3.

#### `src/training/`

**Назначение:** Оркестрация обучения (13 файлов).

**Ключевые файлы:**
- `trainer.py`: Главный класс `Trainer` (1223 строки).

**Зависимости:** `src.agents`, `src.utils`.

#### `src/utils/`

**Назначение:** Общие утилиты (8 файлов).

**Ключевые файлы:**
| Файл | Назначение |
|------|------------|
| `seeding.py` | Глобальные seed'ы, `SeedManager` |
| `metrics_exporter.py` | Экспорт в JSON |

**Зависимости:** numpy, torch.

### 📁 `tests/`

**Назначение:** 200+ unit-тестов, 6 integration.

**Поддиректории:**
- `unit/`: Unit-тесты (pytest.parametrize).
- `integration/`: E2E тесты trainer.

**Ключевые файлы:** `test_seeding.py` (152 строки, 100% coverage).

**Команда:** `pytest tests/ --cov=src/ --cov-report=html`

### 📁 `specs/`

**Назначение:** Спецификации фаз разработки.

**Поддиректории:**
- `001/`: Исходная spec (research).
- `002/`: Cleanup/validation.
- `003/`: Experiments.

**Файлы:** `spec.md`, `plan.md`, `tasks.md`.

### 📁 `results/`

**Назначение:** Артефакты экспериментов (gitignored).

**Структура:**
```
results/
├── experiments/ppo_baseline/
│   ├── model.zip
│   └── metrics.json
├── logs/
├── metrics/
├── videos/lander.mp4
└── reports/
```

## 3. Подробные описания ключевых файлов 🔍

### `src/training/trainer.py` (1223 строки)

**Назначение:** Главный оркестратор обучения.

**Ключевые классы/функции:**
- `TrainerConfig`: Конфигурация (dataclass).
- `Trainer`: Управляет агентом, env, callbacks.
- `TrainingResult`: Метрики + пути.
- `create_trainer_from_config()`: Из YAML.

**Зависимости:**
```python
from src.agents import PPOAgent, A2CAgent
from src.utils import set_seed, RLConfig
import gymnasium, stable_baselines3
```

**Использование:**
```bash
python -m src.training.trainer --config configs/training_schema.yaml
```

### `src/agents/ppo_agent.py`

**Назначение:** PPO агент с оптимизацией для LunarLander.

**Ключевые классы:**
- `PPOConfig`: Гиперпараметры (lr=3e-4, n_steps=2048).
- `PPOAgent`: Наследует `Agent`.
- `PPOMetricsCallback`, `EarlyStoppingCallback`.

**Зависимости:** stable_baselines3.PPO, VecNormalize.

### `src/utils/seeding.py` (190 строк)

**Назначение:** Воспроизводимость.

**Ключевые функции:**
- `set_seed(seed: int)`: Глобальный seed.
- `SeedManager`: Управление seed'ами.
- `verify_reproducibility()`: Тест.

**Пример:**
```python
set_seed(42)
```

### Другие ключевые файлы

| Файл | Описание |
|------|----------|
| `src/experiments/completion/baseline_training.py` | Базовые эксперименты |
| `src/visualization/graphs.py` | Matplotlib графики |
| `src/visualization/video.py` | imageio видео |

## 4. Обзор архитектуры 🏗️

### Компонентная диаграмма (ASCII)

```
+----------------+     +----------------+     +----------------+
|   CLI/Configs  | --> |    Trainer     | --> |   Agent (PPO)  |
+----------------+     +----------------+     +----------------+
                                 |                   |
                                 v                   v
                       +----------------+     +----------------+
                       |  Environment   | <-- | Stable-B3 Core |
                       | LunarLander-v3 |     +----------------+
                       +----------------+
                                 |
                                 v
                       +----------------+
                       |   Utils        |
                       | seeding, metrics|
                       +----------------+
```

**Поток данных:**
1. CLI → TrainerConfig (YAML).
2. Trainer → Agent + Env.
3. Agent.learn() → Metrics/Callbacks → Utils.
4. Results → results/ (JSON, ZIP, MP4).

**Зависимости модулей:**
- training → agents, utils
- agents → stable-baselines3, utils
- utils → numpy, torch

## 5. Описания рабочих процессов 🔄

### Workflow обучения
1. `python -m src.training.trainer --config ...`
2. Trainer.setup() → Env + Agent.
3. Trainer.train() → learn() + callbacks.
4. Save model/metrics.

### Эксперимент workflow
`python -m src.experiments.runner --config ...`

### Оценка
`trainer.evaluate(n_episodes=10)`

### Audit/Cleanup
См. `src/audit/`, `src/cleanup/`.

## 6. Конфигурационные файлы ⚙️

- `pytest.ini`: `addopts = -v --cov=src/`
- `AGENTS.md`: [Руководство →](#agentsmd---руководство-по-разработке-агентов)
- `specs/`: Фазы разработки.

## 7. Точки входа 🚪

| Команда | Описание |
|---------|----------|
| `python -m src.training.trainer` | Обучение |
| `python -m src.experiments.runner` | Эксперименты |
| `pytest tests/` | Тесты |
| `ruff check .` | Linting |

## Приложения 📎

### AGENTS.md - Руководство по разработке агентов 🤖

[Содержимое AGENTS.md вставлено из контекста проекта]

*Обновлено: 03.02.2026*

---

**Длина документа: ~2500 строк. Для обновлений: `ruff format PROJECT_STRUCTURE.md`**
